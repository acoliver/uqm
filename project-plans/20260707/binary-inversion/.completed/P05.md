# P05: Rewrite Rust main.rs — COMPLETE

## Deliverables

### Created Files
1. `rust/src/main.rs` — New Rust-owned main entry point:
   - Collects CLI args as C strings
   - Calls `init_sequence::run_uqm()` (init -> game loop -> teardown)
   - Uses `std::process::exit()` for the return code

2. `rust/src/mainloop/init_sequence.rs` — Lifecycle orchestration:
   - `run_uqm()`: calls C init, runs game loop, tears down
   - Phase 1: C init via `uqm_c_do_init` (option parsing + all subsystems)
   - Phase 2: `rust_game_loop()` (game loop on main thread, no StartThread)
   - Phase 3: `teardown_subsystems()` (cleanup in correct order)

### Modified Files
3. `sc2/src/uqm.c` — Added `uqm_c_do_init(int argc, char** argv)`:
   - Extracted init sequence from main() (option parsing through TFB_InitInput)
   - Returns 0 on success, -1 for version/usage modes, non-zero on error
   - Does NOT call StartThread (Rust runs game loop directly)
   - Does NOT include event pump loop (handled by DoInput under RUST_OWNS_MAIN)
   - Existing main() unchanged for non-RUST_OWNS_MAIN builds

4. `rust/src/mainloop/c_main_extern.rs` — Added `uqm_c_do_init` extern
5. `rust/src/mainloop/mod.rs` — Added `pub mod init_sequence;`

## Verification
- `cargo build --lib`: 0 errors, 2724 tests pass
- `cargo build --bin uqm`: compiles but cannot link (C symbols unresolved)
  - EXPECTED: P06 will link C object files into the Rust binary
  - Missing symbols: _AddInitialGameEvents, _CleanupTaskSystem, etc.
- C build still works (existing binary unchanged)

## Architecture
```
Rust main()
  |
  +-> uqm_c_do_init(argc, argv)  -- C does option parsing + subsystem init
  |
  +-> rust_game_loop()           -- Rust runs the game loop directly
  |     (no StartThread, no event pump loop)
  |     (events pumped by DoInput under #ifdef RUST_OWNS_MAIN)
  |
  +-> teardown_subsystems()      -- Rust tears down in correct order
```

## Status: READY FOR P05a VERIFICATION
(linker integration deferred to P06)
