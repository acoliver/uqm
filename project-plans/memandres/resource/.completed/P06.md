# Phase 06: Color Parser Fix — Stub [OK]

## Phase ID
`PLAN-20260224-RES-SWAP.P06`

## Completion Date
2026-02-24

## What Was Done
- Added `parse_c_color(descriptor: &str) -> Result<(u8, u8, u8, u8), String>` stub with `todo!()`
- Added `serialize_color(r: u8, g: u8, b: u8, a: u8) -> String` stub with `todo!()`
- Marked `ColorResource::from_hex` as `#[deprecated]` with note to use `parse_c_color()`
- Added `#[allow(deprecated)]` to all existing callers of `from_hex` (resource_type.rs, tests.rs, resource_system.rs)
- Plan markers `@plan PLAN-20260224-RES-SWAP.P06` and `@requirement REQ-RES-066-074` present

## Verification
- `cargo test --lib --all-features`: 1185 passed, 0 failed, 28 ignored
- All existing tests still pass
- Stubs compile correctly
