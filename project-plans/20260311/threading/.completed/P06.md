# PLAN-20260314-THREADING.P06 Completion Report

Timestamp: 2026-03-16T16:30:00Z
Phase: P06 (SleepThreadUntil Async Pumping Restoration)
Status: COMPLETE

## Changes Made

### `sc2/src/libs/threads/rust_thrcommon.c`
- Replaced single-shot `SleepThreadUntil` with async-pumping loop
- Implementation matches legacy `thrcommon.c:333-362` exactly
- Uses `Async_process()` → time check → `Async_timeBeforeNextMs()` → clamp → `SleepThread()`
- Overflow-safe time conversion preserved
- `libs/async.h` already included (line 16)

### No Rust changes
- No Rust files modified
- All 1569 Rust tests still pass

## Verification Results

### C Build
- `make -f Makefile.build` — exit 0, no errors or warnings related to `rust_thrcommon.c`

### Rust Tests
- `cargo test --lib --all-features` — 1569 passed, 0 failed, 5 ignored

### Behavioral Validation
- Implementation matches legacy C line-for-line (minus DEBUG_SLEEP which was not in the Rust adapter)
- `Async_process()` called BEFORE time check (line ordering verified)
- Sleep interval bounded by both wake time AND next async event
- Immediate return when `wakeTime <= now`
- Loop continues after each sub-interval sleep

### Structural Checklist
- [x] `Async_process()` call present
- [x] `Async_timeBeforeNextMs()` call present
- [x] `for (;;)` loop (not single-shot)
- [x] Returns when `wakeTime <= now`
- [x] Overflow-safe time conversion formula
- [x] `SleepThread(nextTime - now)` for sub-intervals
- [x] `wakeTime < nextTime` clamp present
- [x] `libs/async.h` included
