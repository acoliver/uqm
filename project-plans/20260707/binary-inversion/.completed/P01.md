# P01: Analysis — PASS

## Files Created
- `plan/01-analysis.md` — Full analysis of C main(), init/teardown sequences, threading model, event pump, DCQ
- `plan/02-pseudocode.md` — Target Rust main.rs structure, FFI extern list, game loop integration
- `plan/03-phases.md` — 7-phase plan (P02-P07 + verification gates)

## Key Findings

### Init Sequence: 25 calls in C main()
- 7 already Rust-backed (mem, threads, IO, time, gfx, comm, input)
- 15 pure C, need FFI externs
- 3 are option-parsing/config (port to Rust or call through C)

### Event Pump: 4 calls in main thread loop
- TFB_ProcessEvents — SDL event polling (needs FFI extern)
- ProcessUtilityKeys — key handling (needs FFI extern)
- ProcessThreadLifecycles — thread reaping (ELIMINATED in single-threaded)
- TFB_FlushGraphics — DCQ drain (needs FFI extern)

### Threading
- StartThread(Starcon2Main) spawns game thread → ELIMINATED
- ProcessThreadLifecycles reaps dead threads → ELIMINATED
- MainExited cross-thread signal → ELIMINATED (direct return)
- initAudio uses AssignTask → keep calling through C for now

### Teardown: 14 calls, all simple, need FFI externs

### Total FFI externs needed: ~30

VERDICT: PASS
