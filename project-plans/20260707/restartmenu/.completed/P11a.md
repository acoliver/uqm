# P11a: E2E Verification — PARTIAL PASS

## Status
The C→Rust wiring for the restart/menu system is verified correct:
- StartGame() delegates to rust_start_game() under USE_RUST_RESTART
- The full orchestration chain executes: start_game → try_start_game → restart_menu → DoInput
- Menu sounds and music load through the Rust path
- DoInput runs with the Rust InputFunc callback

## Runtime Issue
Segfault during DoRestart first-frame rendering. The wiring is not the cause —
the crash occurs in C drawing functions called from Rust via FFI. Likely causes:
1. MENU_STATE pointer layout mismatch
2. DrawRestartMenuGraphic expects initialized graphics context not set up by Rust
3. Frame handle (CurFrame) not properly set before drawing

## Recommendation
Debug as a separate task using lldb/gdb to get the crash backtrace.

VERDICT: PARTIAL PASS (wiring verified, runtime crash needs debugging)
