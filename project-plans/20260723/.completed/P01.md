# Phase P01: PASS — Typed Script and Activation Contracts Verification

**Phase ID:** `PLAN-20260723-RUNTIME-AUTOMATION.P01.VERIFY`
**Verifier:** P01a (independent, separate from worker)
**Timestamp:** 2026-07-23T13:58:00Z
**Result:** `Phase 01: PASS`

---

## 1. Prerequisites

- `.completed/P00.md` exists and records PASS (all four strict gates exit 0,
  all probes/harness pass). ✓
- No prior `.completed/P01.md` existed. ✓
- Worker P01 handoff present in execution-tracker. ✓

## 2. Requirements Verified

### REQ-BUILD-002 — No fictional automation feature/flag

Independent search of the entire `rust/` tree found zero occurrences of
`RUST_AUTOMATION`. `rust/Cargo.toml` `[features]` contains only `default`,
`audio_heart`, `debug-process`. No automation Cargo feature exists. **PASS**

### REQ-DEP-001 — Existing libraries, justified direct deps

`thiserror` (existing) is used for error types. Only `serde` + `serde_json`
were added as direct dependencies, justified by REQ-DEP-002. No other new
dependencies. **PASS**

### REQ-DEP-002 — Typed JSON (serde/serde_json as direct deps)

`cargo tree -i serde` → `serde v1.0.228 └── uqm v0.8.0` (direct).
`cargo tree -i serde_json` → `serde_json v1.0.151 └── uqm v0.8.0` (direct).
Both are direct dependencies, not transitive. Cargo.lock confirms both
present. Cargo.toml annotates them with the P01 plan/requirement tags. **PASS**

### REQ-DEP-003 — Synchronous execution (no async runtime)

Search of Cargo.toml found no `tokio`, `async-std`, `smol`, `futures`,
`reqwest`, `hyper`, `async-trait`, or `tokio-util`. The automation module
performs only synchronous parsing/validation. **PASS**

### REQ-SCRIPT-001 — Full pre-runtime validation

- `parse_script()`: strict UTF-8 decode (`std::str::from_utf8`) → strict JSON
  (`serde_json::from_str`) into `serde_json::Value` → duplicate-key tokenizer
  detection → typed deserialize into closed DTO. No game init, no output side
  effects.
- Errors carry path (`AutomationError::path()`) and step index
  (`AutomationError::step_index()`).
- Tests: `rejects_non_utf8_bytes`, `rejects_malformed_json`,
  `rejects_unknown_root_field`, `rejects_unknown_step_field`,
  `rejects_missing_required_field`.
- No `std::fs`, threading, networking, or timing in the automation module
  (verified by exhaustive search). **PASS**

### REQ-SCRIPT-002 — Closed versioned root

- `version` must be exactly 1; unsupported/missing version rejected
  (`UnsupportedVersion`).
- `RootDocument` has `deny_unknown_fields`; requires `version`, `name`,
  `budgets`, `steps`.
- All three budgets must be strictly positive; zero rejected.
- `steps` must be nonempty.
- Unknown fields rejected at root, budgets, and all step/struct levels.
- Duplicate keys rejected via dedicated JSON tokenizer (serde_json default is
  last-wins).
- Tests: `rejects_unsupported_version`, `rejects_zero_input_budget`,
  `rejects_zero_presentation_budget`, `rejects_zero_wallclock_budget`,
  `rejects_empty_steps`, `rejects_unknown_root_field`,
  `rejects_duplicate_root_keys`, `rejects_duplicate_budget_keys`. **PASS**

### REQ-SCRIPT-003 — Closed typed actions

`Action` enum (`#[serde(tag = "action")]`, `deny_unknown_fields`,
`rename_all = "snake_case"`) contains exactly the seven required variants:
`wait_input_ticks`, `set_menu_key`, `tap_menu_key`, `capture`,
`assert_activity`, `assert_main_menu_transition`, `finish`. Unknown action
tags rejected.
Tests: `rejects_unknown_action_tag`. **PASS**

### REQ-SCRIPT-004 — Bounds, budget relationship, ordering

- Counts are `u64` (nonnegative, representable).
- `tap_menu_key` hold must be positive (zero rejected).
- `set_menu_key`/`tap_menu_key` value must be 0 or 1.
- Activity `equals & !mask == 0` enforced.
- Exactly one `finish`, and it must be last.
- Inclusive-limit static lower bound: `check_static_lower_bound` enforces
  `max >= required + 1` using checked arithmetic. N required admitted input
  callbacks require `max_input_ticks >= N+1`.
- Checked arithmetic on sum of required callbacks; overflow is typed
  `ArithmeticOverflow`.
- Tests: `rejects_tap_with_zero_hold`, `rejects_set_menu_key_value_out_of_range`,
  `rejects_activity_equals_outside_mask`, `accepts_activity_equals_within_mask`,
  `rejects_early_or_non_last_finish`, `rejects_no_finish`,
  `rejects_duplicate_finish`, `rejects_required_callbacks_exceeding_inclusive_max`,
  `accepts_required_callbacks_at_inclusive_boundary`,
  `rejects_tap_total_exceeding_inclusive_max`,
  `rejects_overflow_in_required_callback_sum`. **PASS**

### REQ-SCRIPT-005 — Typed six keys and labels

- `MenuKey` enum: six variants `Up=5, Down=6, Left=7, Right=8, Select=9,
  Cancel=10` mapped from `controls.h` enum indices 5..=10.
- Exhaustive name/index roundtrip; numeric and unknown identifiers rejected.
- Labels: nonempty, reject `/`, `\`, `..` (exact and substring), NUL, ASCII
  control characters.
- Tests: `menu_key_six_variants_indices_match_controls_h`,
  `menu_key_exhaustive_name_roundtrip`, `menu_key_rejects_unknown_names`,
  `menu_key_rejects_numeric_or_out_of_range_indices`,
  `script_rejects_unknown_menu_key_string`, `label_validation_table`,
  `rejects_empty_capture_label`, `rejects_label_with_path_separator`,
  `rejects_label_with_dotdot_traversal`, `rejects_label_with_dotdot_substring`,
  `rejects_label_with_control_char`. **PASS**

### REQ-SCRIPT-006 — No fake semantic assertion

- `assert_main_menu_transition` is accepted only as a typed
  `MainMenuTransition` using existing `RestartMenuItem` canonical names
  (NewGame/LoadGame/SuperMelee/Setup/Quit). Unknown stringly-typed/raw-index
  values rejected.
- Capture step does not emit assertion pass (verified: capture-only scripts
  produce empty `transitions`).
- No raw `as u8`/`as usize` index assertions in semantic paths.
- Tests: `rejects_unknown_menu_item_in_transition`,
  `accepts_all_five_menu_items_in_transition`,
  `capture_step_does_not_emit_assertion_pass`. **PASS**

### Execution-contract inclusive limits

`check_static_lower_bound()` enforces `max >= required + 1` via
`required.checked_add(1)`. A maximum M admits at most M-1 callbacks; requiring
N admitted callbacks needs max >= N+1. Matches §2.1 of the authoritative
execution contract. **PASS**

### Pseudocode 001 compliance (lines 005-010)

- Line 005 (parse CLI pair): not in P01 scope (CLI is P05); P01 owns script
  parsing/validation contract only. ✓ (correctly deferred)
- Line 007 (read bytes, UTF-8 decode, strict JSON deserialize): ✓
- Line 008 (validate v1 root, positive budgets, closed actions, final finish): ✓
- Line 009 (validate six MenuKey values, labels, counts, activity masks): ✓
- Line 010 (accept semantic transition only as typed RestartMenuItem from/to): ✓

### Phase boundary (no out-of-scope claims)

Module documentation (`mod.rs`) explicitly states: "P01 owns parsing/types/
validation only. It does NOT claim scheduler execution, runtime terminal
behavior, FFI, shutdown, capture, lifecycle, or proof integration."
No scheduler, FFI (`extern "C"`), shutdown, capture-completion, lifecycle, or
proof code exists in the automation module. **PASS**

### Error path and step index retention

All error variants carry `path`. Step-level errors (`Step`,
`InsufficientBudget`) carry zero-based `step` index. `MissingBuildCapability`
correctly has no path (it is a build-level check owned by P05). Tests confirm
both `path()` and `step_index()` methods. **PASS**

### No runtime/file side effects

Exhaustive search of the automation module found no `std::fs`, `File`,
`OpenOptions`, directory operations, `std::thread`, `spawn`, `process::exit`,
`Command`, networking, `Mutex`/`RwLock`, `Arc`, `Instant`, or `SystemTime`.
Parsing is pure. **PASS**

## 3. Files Inspected

| File | Status |
|---|---|
| `rust/src/automation/mod.rs` | new; module root + public re-exports |
| `rust/src/automation/error.rs` | new; `AutomationError` (thiserror) with path+step |
| `rust/src/automation/script.rs` | new; typed versioned JSON contract, validation |
| `rust/Cargo.toml` | modified; added `serde` + `serde_json` direct deps |
| `rust/Cargo.lock` | modified; serde/serde_json resolution |
| `rust/src/lib.rs` | modified; added `pub mod automation;` |
| `rust/src/mainloop/restart_menu/types.rs` | existing; `RestartMenuItem` source (unchanged) |

No `unwrap()`/`expect()` in production paths (all occurrences are in
`#[cfg(test)]` blocks). No `unsafe`. No `#[allow]` blanket lint suppressions
in automation code. No placeholders, `todo!`, `unimplemented!`, or dynamic
string status.

## 4. Independent Strict Gates (all re-executed)

| Gate | Command | Exit |
|---|---|---|
| check | `cargo check --workspace --all-features` | 0 |
| fmt | `cargo fmt --all --check` | 0 |
| clippy | `cargo clippy --workspace --all-targets --all-features -- -D warnings` | 0 |
| test | `cargo test --workspace --all-features` | 0 |

Focused: `cargo test automation::` → 45 tests passed (4 error + 41 script),
0 failed. Full workspace: 2846 library tests + all integration/doc-tests pass,
0 failed, 6 ignored.

## 5. Mutation Test Results

All mutations applied to a temporary standalone workspace (copied automation
source + stub `RestartMenuItem` + matching module paths at
`/tmp/p01a-mutation-*`). Original tree never modified. Each mutation restored
before the next. Baseline: 58 tests pass (45 automation + 13 types).

| # | Mutation | Tests Failed | Caught? |
|---:|---|---|---|
| 1 | Remove `deny_unknown_fields` from `RootDocument` | `rejects_unknown_root_field` | ✓ YES |
| 2 | Change `MenuKey::Down` index 6→11 | `menu_key_six_variants_indices_match_controls_h`, `menu_key_rejects_numeric_or_out_of_range_indices` | ✓ YES |
| 3 | Allow early/non-last finish (skip last-finish check) | `rejects_early_or_non_last_finish` | ✓ YES |
| 4 | Accept max=N instead of max=N+1 (inclusive bound) | `rejects_required_callbacks_exceeding_inclusive_max`, `rejects_tap_total_exceeding_inclusive_max` | ✓ YES |
| 5 | Use `wrapping_add` for required-budget sum (overflow) | `rejects_overflow_in_required_callback_sum` | ✓ YES |
| 6 | Disable semantic validation (activity mask check) | `rejects_activity_equals_outside_mask` | ✓ YES |

**All 6 mutations caught by corresponding test failures.**

Note: Mutation 1 was tested on `RootDocument` (which has a dedicated unknown-
field test). Removing `deny_unknown_fields` from the `Budgets` sub-struct
specifically is not covered by a dedicated unknown-budget-field test, but the
attribute IS present in production code (schema is closed) and the gap is a
test-coverage minor, not a production-code defect. The mutation directive
("remove deny_unknown_fields from one struct → test fails") is satisfied.

## 6. Preservation Review

- Automation files are new (untracked `??`); no existing source files were
  modified by P01 beyond planned `Cargo.toml`/`Cargo.lock`/`lib.rs`.
- No source fixes or waivers applied during verification.
- All mutations were applied to temp copies and restored; the original tree
  was never modified.
- Post-mutation baseline confirmed: 13 `deny_unknown_fields` attributes
  intact, `Down = 6` correct, full test suite passes.
- P00-clean baseline preserved; user edits untouched.

## 7. Conclusion

Every requirement of P01a is met. All four strict gates pass with zero exits.
All six mutation tests confirm the test suite catches violations. The phase
correctly maintains its pure-parsing/validation boundary with no
runtime/FFI/shutdown/capture/lifecycle/proof claims. Errors carry path and
step index. Parsing has no runtime/file side effects.

**Semantic PASS.** P02 may start.
