# Phase P00a Report: Preflight Verification

## Phase ID
`PLAN-20260314-THREADING.P00a`

## Result
**PASS**

## Summary
All checklist items were verified against the current source tree and command output. The Rust toolchain is present, the Rust workspace builds successfully, library tests pass with the expected scale, and the documented threading gaps G1/G2/G3/G4 are confirmed by direct source inspection rather than contradicted by the current code.

## Evidence by Checklist Item

### 1. TOOLCHAIN

- **PASS** — `cargo --version`
  - Command run in `/Users/acoliver/projects/uqm/rust`
  - Output: `cargo 1.92.0 (Homebrew)`
  - Satisfies minimum 1.70+ expectation.

- **PASS** — `rustc --version`
  - Output: `rustc 1.92.0 (ded5c06cf 2025-12-08) (Homebrew)`
  - Satisfies minimum 1.70+ expectation.

- **PASS** — `cargo clippy --version`
  - Output: `clippy 0.1.92`

### 2. BUILD

- **PASS** — `cd /Users/acoliver/projects/uqm/rust && cargo build 2>&1`
  - Verified by command execution.
  - Final summary:
    - `warning: 'uqm' (lib) generated 258 warnings`
    - `Finished 'dev' profile [unoptimized + debuginfo] target(s) in 0.07s`
  - Exit code: `0`
  - Build succeeds, though with many pre-existing warnings.

### 3. TESTS

- **PASS** — `cd /Users/acoliver/projects/uqm/rust && cargo test --lib --all-features 2>&1`
  - Verified by command execution.
  - Final summary:
    - `test result: ok. 1560 passed; 0 failed; 5 ignored; 0 measured; 0 filtered out; finished in 0.11s`
  - Exit code: `0`

- **PASS** — Exact test count recorded
  - Exact passed count: **1560**
  - Ignored: **5**
  - Confirms expected `1560+` threshold.

### 4. TYPES

Source inspected: `/Users/acoliver/projects/uqm/rust/src/threading/mod.rs`

- **PASS** — `Thread<T>` exists with expected fields
  - `pub struct Thread<T>` at lines 81-85
  - Fields:
    - `handle: Option<JoinHandle<T>>` at line 82
    - `name: Option<String>` at line 84

- **PASS** — `RustFfiMutex` exists with expected fields
  - `struct RustFfiMutex` at lines 212-217
  - Fields:
    - `state: Mutex<FfiMutexState>` at line 213
    - `condvar: Condvar` at line 214
    - `name: Option<String>` at line 216

- **PASS** — `UqmCondVar` exists with expected fields
  - `pub struct UqmCondVar` at lines 303-309
  - Fields:
    - `inner: Condvar` at line 304
    - `state: Mutex<CondVarState>` at line 306
    - `name: Option<String>` at line 308

- **PASS** — `Semaphore` exists with expected fields
  - `pub struct Semaphore` at lines 452-457
  - Fields:
    - `count: Mutex<u32>` at line 453
    - `condvar: Condvar` at line 454
    - `name: Option<String>` at line 456

- **PASS** — `FfiThreadLocal` exists and is `#[repr(C)]`
  - `#[repr(C)]` at line 730
  - `struct FfiThreadLocal` at lines 731-733
  - Field:
    - `flush_sem: *mut c_void` at line 732

- **PASS** — `ThreadLocalGuard` exists with expected fields
  - `struct ThreadLocalGuard` at lines 793-796
  - Fields:
    - `created_rust: bool` at line 794
    - `created_ffi: bool` at line 795

- **PASS** — `spawn_c_thread` currently returns `Result<Thread<()>>`
  - Function signature at lines 936-940
  - Confirms documented G1 gap note: current type is `Thread<()>`, not `Thread<c_int>`.

### 5. FFI

Source inspected: `/Users/acoliver/projects/uqm/rust/src/threading/mod.rs`

- **PASS** — `rust_thread_spawn` signature matches checklist
  - `#[no_mangle]` at line 953
  - Signature at lines 954-958:
    - `(name: *const c_char, func: unsafe extern "C" fn(*mut c_void) -> c_int, data: *mut c_void) -> *mut RustThread`

- **PASS** — `rust_thread_spawn_detached` signature matches checklist
  - `#[no_mangle]` at line 971
  - Signature at lines 972-976:
    - `(name: *const c_char, func: unsafe extern "C" fn(*mut c_void) -> c_int, data: *mut c_void)`
  - Void return confirmed.

- **PASS** — `rust_thread_join` currently returns only `c_int`
  - `#[no_mangle]` at line 997
  - Signature at line 998:
    - `(thread: *mut RustThread) -> c_int`
  - Confirms documented G1 gap: no `out_status` parameter exists yet.

- **PASS** — `rust_hibernate_thread` signature matches checklist
  - `#[no_mangle]` at line 1026
  - Signature at line 1027:
    - `(msecs: u32)`
  - Void return confirmed.

- **PASS** — `rust_task_switch` signature matches checklist
  - `#[no_mangle]` at line 1402
  - Signature at line 1403:
    - `pub extern "C" fn rust_task_switch()`
  - Void return confirmed.

### 6. C-SIDE

Sources inspected:
- `/Users/acoliver/projects/uqm/sc2/src/libs/threads/rust_thrcommon.c`
- `/Users/acoliver/projects/uqm/sc2/src/libs/threads/rust_threads.h`

- **PASS** — `TrueThread` struct exists with expected fields
  - `rust_thrcommon.c` lines 23-28
  - Fields:
    - `native: RustThread *` at line 24
    - `name: const char *` at line 26 (under `#ifdef NAMED_SYNCHRO`)

- **PASS** — `ThreadStartInfo` exists with expected fields
  - `rust_thrcommon.c` lines 30-34
  - Fields:
    - `func` at line 31
    - `data` at line 32
    - `thread` at line 33

- **PASS** — `RustThreadHelper` captures and returns worker result
  - Function at lines 94-117
  - `result = (*func) (data);` at line 104
  - `return result;` at line 116

- **PASS** — `WaitThread` currently writes `rust_thread_join` result to `*status`
  - Function at lines 208-223
  - `int result = rust_thread_join (t->native);` at line 218
  - `*status = result;` at line 220
  - Confirms G1 gap: this is success/failure, not worker return status.

- **PASS** — `SleepThreadUntil` currently computes delta only
  - Function at lines 192-200
  - Reads current time at line 195 and calls `SleepThread(wakeTime - now)` at line 198
  - No async loop present here.
  - Confirms G2 gap.

- **PASS** — `StartThread_Core` uses `rust_thread_spawn`, not detached spawn
  - Function at lines 164-183
  - `thread->native = rust_thread_spawn (name, RustThreadHelper, startInfo);` at line 177
  - Confirms current G3/G4 assumption.

- **PASS** — C header declarations match current Rust export shapes
  - `rust_threads.h` line 33: `rust_thread_spawn`
  - `rust_threads.h` line 34: `rust_thread_spawn_detached`
  - `rust_threads.h` line 36: `rust_thread_join(RustThread* thread)`
  - Confirms G1 gap note: no `out_status` parameter in header either.

### 7. CALL PATHS

#### G1: Return value propagation path

- **PASS** — C worker function returns `int` via helper
  - `rust_thrcommon.c` line 104: `result = (*func) (data);`
  - `rust_thrcommon.c` line 116: `return result;`

- **PASS** — Rust closure discards return value
  - `mod.rs` lines 944-950
  - `let _ = unsafe { func(data) };` at line 949
  - Confirms exact G1 gap.

- **PASS** — `Thread<()>` is used, preventing status recovery
  - `spawn_c_thread` returns `Result<Thread<()>>` at line 940
  - Join handle boxed as `Box<Thread<()>>` in `rust_thread_join` at line 1003

- **PASS** — `rust_thread_join` can only report success/failure
  - `match thread.join()` at lines 1004-1007
  - Returns `1` on `Ok(())`, `0` on `Err(_)`

- **PASS** — `WaitThread` stores boolean join result into `*status`
  - `rust_thrcommon.c` lines 218-220

- **PASS** — Proposed fix path is consistent with current code shape
  - Evidence supports need to change:
    - `spawn_c_thread` to `Thread<c_int>`
    - closure to return captured `c_int`
    - `rust_thread_join` ABI to include `out_status`

#### G2: SleepThreadUntil async pumping path

- **PASS** — Legacy path pumps async work in loop
  - `thrcommon.c` lines 333-362 inspected
  - `Async_process ();` at line 348
  - `Async_timeBeforeNextMs ();` at line 354

- **PASS** — `Async_process` is declared
  - `sc2/src/libs/callback/async.h` line 24

- **PASS** — `Async_timeBeforeNextMs` is declared
  - `sc2/src/libs/callback/async.h` line 25

- **PASS** — `rust_thrcommon.c` includes async header
  - `#include "libs/async.h"` at line 16

- **PASS** — current Rust-thread wrapper path does not pump async work
  - `rust_thrcommon.c` lines 192-200 contain only time delta calculation and sleep.

- **PASS** — Fix is entirely on C side
  - All missing behavior for G2 is in `rust_thrcommon.c`; no Rust FFI changes are required to add the loop.

#### G3/G4: Detached spawn path

- **PASS** — `rust_thread_spawn_detached` exists and drops result
  - `mod.rs` lines 972-984
  - `let _ = spawn_c_thread(name_str, func, data);` at line 983

- **PASS** — dropped result is `Result<Thread<()>>`
  - From `spawn_c_thread` signature at line 940

- **PASS** — dropping handle loses return value and does not join
  - Current detached function does not store or join the handle; source confirms that behavior.

- **PASS** — `StartThread_Core` currently uses non-detached spawn
  - `rust_thrcommon.c` line 177 uses `rust_thread_spawn`

- **PASS** — current state matches stated design decision
  - Code confirms the plan’s claim that `StartThread_Core` intentionally remains on non-detached path for now.

### 8. TEST INFRA

Source inspected: `/Users/acoliver/projects/uqm/rust/src/threading/tests.rs`

- **PASS** — Test file exists
  - File exists at `/Users/acoliver/projects/uqm/rust/src/threading/tests.rs`

- **PASS** — Tests module is included from `mod.rs`
  - `#[cfg(test)] mod tests;` at lines 24-25 of `mod.rs`

- **PASS** — Existing test categories cover requested areas
  - Header comments list categories at tests.rs lines 6-11:
    - thread tests
    - mutex tests
    - condition variable tests
    - semaphore tests
    - task tests
  - Additional sections present in file:
    - thread system tests at lines 592-656
    - thread-local storage tests at lines 658-697
    - error handling tests at lines 699-726
  - Coverage evidence:
    - thread spawn/join: lines 28-143
    - mutex: lines 150-255
    - condvar: lines 261-355
    - semaphore: lines 361-500
    - task: lines 506-590
    - system init: lines 596-616
    - sleep/yield: lines 618-656
    - TLS: lines 662-697
    - error handling: lines 703-726

- **PASS** — New G1 tests are not expected yet
  - No current C-FFI return propagation test is present; only generic Rust return-value coverage exists.

- **PASS** — Existing tests use both `Thread<()>` and `Thread<i32>` patterns
  - `test_thread_spawn_and_join` uses a closure returning `()` at lines 42-47
  - `test_thread_spawn_with_return_value` uses a closure returning integer at lines 68-79

### 9. TODOs

Source inspected: `/Users/acoliver/projects/uqm/rust/src/threading/mod.rs`

- **PASS** — `mod.rs:596` TODO exists, but state retrieval is implemented immediately below
  - TODO at line 596: `// TODO: Implement state retrieval`
  - Actual implementation uses `self.state.load(Ordering::SeqCst)` at line 597 and maps values through line 603.

- **PASS** — `mod.rs:611` TODO exists, but state setting is implemented immediately below
  - TODO at line 611: `// TODO: Implement state setting`
  - Actual implementation stores with `self.state.store(state as u32, Ordering::SeqCst);` at line 612.

- **PASS** — `mod.rs:681` TODO exists for lifecycle processing
  - TODO at line 681: `// TODO: Implement lifecycle processing`
  - Function body is otherwise intentionally empty except comment at lines 682-683.
  - Matches checklist note that this remains C-owned per G6.

- **PASS** — `mod.rs:697` TODO exists for hibernation, but sleep call is already implementation
  - TODO at line 697: `// TODO: Implement thread hibernation`
  - Actual implementation uses `thread::sleep(duration);` at line 699.
  - Confirms checklist note for G5.

## Dependency Verification

- **PASS** — `std::thread` / `std::sync` are used from the standard library
  - Imports at `mod.rs` lines 27-29

- **PASS** — `std::ffi` is used from the standard library
  - Import at `mod.rs` line 831: `use std::ffi::{c_char, c_int, c_void, CStr};`

- **PASS** — No new crate dependency is required by this plan
  - The verified threading implementation and identified gaps are all within std + existing project code.
  - No checklist item or inspected code indicates a missing third-party crate requirement.

## Blocking Issues

None blocking Phase 01.

Notes for later phases:
- G1 is real: Rust FFI thread path currently discards worker `c_int` and only returns join success/failure.
- G2 is real: `rust_thrcommon.c::SleepThreadUntil` lacks legacy async pumping loop.
- `cargo build` succeeds but emits many existing warnings unrelated to this gate.

## Gate Decision

- **PASS** — proceed to Phase 01
