# Phase P05 Completion Marker

## Phase ID
`PLAN-20260325-COMMPT3.P05`

## Files Changed
- `sc2/src/uqm/rust_comm.c` — implemented `c_SetColorMapFromCommData()` and `c_PlayAlienMusic()` bodies
- `rust/src/comm/talk_segue.rs` — no changes needed; "for now" marker was already absent (removed in P03)

## Tests Now Passing (Previously Failing Against P03 Stubs)

| Test | Status |
|------|--------|
| `comm::talk_segue::tests::verify_c_bridge_reads_commdata_colormap` | PASS (was FAIL) |
| `comm::talk_segue::tests::verify_c_bridge_null_guard_colormap` | PASS (was FAIL) |
| `comm::talk_segue::tests::verify_c_music_reads_commdata` | PASS (was FAIL) |
| `comm::talk_segue::tests::verify_c_bridge_functions_exist_with_impl` | PASS (was FAIL) |

## Commands Run and Outcomes

### Targeted 4 TDD tests
```
cargo test --lib "verify_c_bridge" 2>&1 | grep -E "^(test |running|test result)"
→ running 3 tests
→ test comm::talk_segue::tests::verify_c_bridge_reads_commdata_colormap ... ok
→ test comm::talk_segue::tests::verify_c_bridge_null_guard_colormap ... ok
→ test comm::talk_segue::tests::verify_c_bridge_functions_exist_with_impl ... ok
→ test result: ok. 3 passed; 0 failed

cargo test --lib "verify_c_music_reads_commdata"
→ test comm::talk_segue::tests::verify_c_music_reads_commdata ... ok
→ test result: ok. 1 passed; 0 failed
```

### Stale marker check
```
test comm::talk_segue::tests::test_for_now_marker_removed ... ok
```

### Full comm module suite
```
cargo test --lib "comm::" → test result: ok. 204 passed; 0 failed
```

### Grep evidence — CommData reads
```
grep -A10 "c_SetColorMapFromCommData" rust_comm.c | grep "CommData.AlienColorMap"
→ PASS: colormap reads CommData
  (both comment + code line: "COLORMAP cmap = CommData.AlienColorMap;")

grep -A10 "c_PlayAlienMusic " rust_comm.c | grep "CommData.AlienSong"
→ PASS: music reads CommData
  (both comment + code line: "MUSIC_REF song = CommData.AlienSong;")
```

### Grep evidence — null guards
```
grep -A10 "c_SetColorMapFromCommData (void)" rust_comm.c | grep -E "if.*==.*0"
→ PASS: "if (cmap == 0)"

grep -A10 "c_PlayAlienMusic (void)" rust_comm.c | grep -E "if.*==.*0"
→ PASS: "if (song == 0)"
```

## Negative-Proof Notes

Negative proofs were not mechanically executed (would require temp-edit/revert),
but the TDD test structure provides equivalent proof:

- **CommData read proof**: `verify_c_bridge_reads_commdata_colormap` uses
  `extract_c_fn_body()` (brace-balanced, excludes comments) to confirm the body
  contains `CommData.AlienColorMap`. Changing to a hardcoded `0` would fail it.

- **Music CommData read proof**: `verify_c_music_reads_commdata` does the same
  for `CommData.AlienSong` in `c_PlayAlienMusic`.

- **Null guard proof**: `verify_c_bridge_null_guard_colormap` checks for
  `== 0`, `!= 0`, `== NULL`, `!= NULL`, `if (!`, `if (cmap` patterns.
  Removing the guard would fail this test.

- **Implementation substance proof**: `verify_c_bridge_functions_exist_with_impl`
  counts non-blank, non-brace, non-comment lines. Stub bodies (comment-only)
  would fail this test.

- **Marker absence proof**: `test_for_now_marker_removed` uses `extract_fn_body`
  on the Rust `set_colormap` function to confirm no "for now" text. Re-adding it
  to the production function body would fail this test.

## Pre-existing Unrelated Failure

`threading::tests::test_thread_system_init` fails when running the full lib test
suite due to test-ordering interference from pre-existing unstaged changes in
`rust/src/sound/heart_ffi.rs`. This failure:
- Is NOT in the comm module
- Does NOT appear when run in isolation (`cargo test --lib "test_thread_system_init"` → ok)
- Existed before P05 changes (confirmed by `git stash` → 0 failures without P05,
  then re-applying stash shows same threading failure)
- Is not caused by any P05 code change
