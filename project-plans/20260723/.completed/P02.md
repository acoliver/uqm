# Phase 02a: Verify Pure Scheduler and Watchdog — PASS

Phase ID: `PLAN-20260723-RUNTIME-AUTOMATION.P02.VERIFY`
Result: **PASS**
Date: 2026-07-23

## Summary

Independently verified `REQ-SCHED-001..003`, `REQ-DET-001`, `REQ-WATCH-001..003` against
execution-contract §2 (exact reducer tables/timelines, capture generation). All requirements
pass. No source fixes or waivers applied. All mutations performed in `/tmp` copies only.

## File scope

- `rust/src/automation/scheduler.rs` — pure scheduler reducer (§2.3 table, §2.4 capture gen)
- `rust/src/automation/watchdog.rs` — pure watchdog reducer (§2.1, §2.2 table)
- `rust/src/automation/mod.rs` — module root (no side effects)
- `rust/src/automation/error.rs` — error types (P01-owned, inspected for purity)
- `rust/src/automation/script.rs` — script types (P01-owned, inspected for typed integration)

No C/SDL/FFI/filesystem/global/lifecycle integration found in any P02 file.
All modules are pure typed reducers with no side effects.

## Strict gates (all exit 0)

| Gate | Command | Exit |
|------|---------|-----|
| 1 | `cargo check --workspace --all-features` | 0 |
| 2 | `cargo fmt --all --check` | 0 |
| 3 | `cargo clippy --workspace --all-targets --all-features -- -D warnings` | 0 |
| 4 | `cargo test --lib --workspace --all-features` | 0 (2890 passed, 0 failed, 6 ignored) |

Note: `cargo test --lib` used per directive (binary has known transitional link failure).

## Focused test results

| Suite | Tests | Result |
|-------|-------|--------|
| `automation::scheduler` (unit + property) | 30 | all pass |
| `automation::watchdog` | 14 | all pass |
| `automation::scheduler::property_tests` (256 cases) | 3 | all pass |
| **Total automation** | **44** | **all pass** |

## Table/timeline coverage

### Watchdog timelines (§2.1, §2.2)

| Table | max | Callback kind | Rows verified | Method |
|-------|-----|---------------|---------------|--------|
| max=1 input | 1 | Input | callback 1 → InputTimeout (M admits 0) | independent test |
| max=1 present | 1 | Present | callback 1 → PresentationTimeout | independent test |
| max=3 input | 3 | Input | cb1→Admit, cb2→Admit, cb3→InputTimeout | independent test + existing tests |
| max=3 present | 3 | Present | cb1→Admit, cb2→Admit, cb3→PresentationTimeout | independent test + existing tests |
| Simultaneous limits (input cb) | 3 | Input | both at boundary → InputTimeout (priority) | independent test |
| Simultaneous limits (present cb) | 3 | Present | both at boundary → PresentationTimeout | independent test |
| Applicable overflow | MAX | Input | u64::MAX → InputCounterOverflow | independent test + existing |
| Applicable overflow | MAX | Present | u64::MAX → PresentationCounterOverflow | independent test + existing |
| Terminal no-increment | 3 | Input | terminal cb doesn't increment other counter | independent test + existing |
| Boundary after release | 2 | Input | boundary cb → terminal, no scheduler work | independent test |

### Scheduler reducer table (§2.3)

| Row | Test | Status |
|-----|------|--------|
| Ready `wait_input_ticks(0)` → advance | `wait_zero_advances_immediately` | PASS |
| Ready `wait_input_ticks(n>0)` → consume n | `wait_n_consumes_n_callbacks` | PASS |
| `set_menu_key(k,v)` → write + advance | `set_menu_key_writes_and_advances` | PASS |
| `tap Hold(n>1)` → held + Hold(n-1) | `tap_hold3_settle2` | PASS |
| `tap Hold(1)` → held + ReleasePending | `tap_hold1_settle0` | PASS |
| `ReleasePending` → release + Settle/advance | `tap_hold1_settle0`, `tap_hold3_settle2` | PASS |
| `Settle(n>0)` → consume, decrement | `tap_settle_consumes_exactly_m_callbacks` | PASS |
| Ready capture → arm + WaitingCapture | `capture_arms_then_completes_on_matching_present` | PASS |
| WaitingCapture + Input → no advance | `waiting_capture_input_does_not_advance` | PASS |
| WaitingCapture + matching Present → complete + advance | `capture_arms_then_completes_on_matching_present` | PASS |
| Semantic wait → exact match advances | `semantic_match_advances` | PASS |
| Semantic mismatch → terminal | `semantic_mismatch_is_terminal` | PASS |
| Finish → terminal success | `finish_is_terminal_success` | PASS |
| Terminal → absorbing | `terminal_is_absorbing` | PASS |
| AssertActivity → immediate advance | `assert_activity_advances` | PASS |

### Capture generation (§2.4)

| Property | Test | Status |
|----------|------|--------|
| Nonzero generation when armed | `capture_generation_is_nonzero_when_armed` | PASS |
| Monotonic (2nd capture = gen 2) | `second_capture_generation_is_2` | PASS |
| Stale (less than pending) rejected | `stale_generation_rejected_with_nonzero_gens` | PASS |
| Duplicate rejected | `duplicate_generation_rejected` | PASS |
| Zero rejected | `zero_generation_rejected` | PASS |
| Future rejected | `future_generation_rejected` | PASS |
| Stale when nothing pending | `stale_when_nothing_pending` | PASS |
| Overflow → typed terminal | `capture_generation_overflow_terminal` | PASS |
| No re-arm on extra input | `waiting_capture_extra_input_no_rearm` | PASS |
| Input independent of present count | `input_independent_of_present_count` | PASS |

### Checked arithmetic (REQ-SCHED-003)

| Property | Test | Status |
|----------|------|--------|
| state_version overflow → typed terminal | `state_version_overflow_terminal` | PASS |
| capture_generation overflow → typed terminal | `capture_generation_overflow_terminal` | PASS |
| input counter overflow → InputCounterOverflow | `input_overflow_is_typed` | PASS |
| present counter overflow → PresentationCounterOverflow | `present_overflow_is_typed` | PASS |
| No wrapping/silent fallback (proptest) | `state_never_panics` (256 cases) | PASS |

## Mutation test results

All 20 mutations performed in `/tmp/p02a-mutations/` copies. Each mutation applied
to a copy of the source, tests run against the mutated copy, then original restored.

| ID | Mutation | Expected | Result |
|----|----------|----------|--------|
| M1 | post-increment after comparison (input) | tests fail | CAUGHT (2 failed) |
| M2 | exact-limit `>=` → `>` (input) | tests fail | CAUGHT (2 failed) |
| M3 | exact-limit `>=` → `>` (presentation) | tests fail | CAUGHT (2 failed) |
| M4 | wall `>=` → `>` | tests fail | CAUGHT (1 failed) |
| M5 | priority: input returns PresentationTimeout | tests fail | CAUGHT (2 failed) |
| M6 | input overflow wraps (wrapping_add) | tests fail | CAUGHT (1 failed) |
| M7 | present overflow wraps | compile error | CAUGHT (type mismatch) |
| M8 | tap release delayed (settle+1) | tests fail | CAUGHT (3 failed) |
| M9 | settle off-by-one (consume too few) | tests fail | CAUGHT (3 failed) |
| M10 | capture generation always zero | tests fail | CAUGHT (4 failed) |
| M11 | capture arms twice (WaitingCapture re-arm) | tests fail | CAUGHT (1 failed) |
| M12 | terminal not absorbing (check removed) | tests fail | CAUGHT (2 failed) |
| M13 | state_version wraps (wrapping_add) | tests fail | CAUGHT (1 failed) |
| M14 | semantic match always true | tests fail | CAUGHT (1 failed) |
| M15 | capture duplicate accepted (false→true) | tests fail | CAUGHT (2 failed) |
| M16 | capture stale accepted (Less→Match) | tests fail | CAUGHT (1 failed) |
| M17 | capture future accepted (Greater→Match) | tests fail | CAUGHT (1 failed) |
| M18 | capture zero accepted (Zero→Match) | tests fail | CAUGHT (1 failed) |
| M19 | duplicate check skipped (already_completed→false) | tests fail | CAUGHT (1 failed) |
| M20 | hold decrement off-by-one (sub(2)) | tests fail | CAUGHT (2 failed) |

**All 20 mutations caught.** No mutation survived.

## Property test evidence

| Property | Cases | Result |
|----------|-------|--------|
| `state_never_panics` (no counter wrap) | 256 | PASS |
| `terminal_absorbing` (terminal is sticky) | 256 | PASS |
| `deterministic_replay` (same events → same states) | 256 | PASS |

No proptest failures; no seed needed.

## Purity inspection

- No `unsafe` in any P02 file
- No `unwrap()`/`expect()` in production paths (test-only `unwrap()`/`expect()` in test assertions)
- No SDL/FFI/extern references
- No filesystem/file I/O
- No global state or static mutables
- No threads, locks, mutexes, atomics in P02 source (capture generation model uses typed `CaptureGeneration`, not actual atomics)
- No process::exit, no lifecycle integration
- No placeholders or TODOs
- No arbitrary sleep or wall-clock dependencies (Instant used only in watchdog tests, not in production reducer logic — reducer takes `Duration`/`ClockSample` as typed inputs)
- No flaky wall-clock tests (all tests use fixed `Instant::now()` baselines with deterministic offsets)

## Preservation result

- All P00/P01/user edits preserved
- No reset/restore/revert/checkout performed
- No source modifications applied (verification only)
- Mutation tests performed exclusively in `/tmp/p02a-mutations/` copies
- Temporary verification test files created and removed from `tests/` directory
- `git diff` confirms automation source files unmodified
- No `.tmp` files left in source tree