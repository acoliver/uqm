# Phase P00: PASS — Clean Baseline and Source Preflight

**Phase ID:** `PLAN-20260723-RUNTIME-AUTOMATION.P00.VERIFY`
**Verifier:** P00a (independent, separate from worker)
**Timestamp:** 2026-07-23T13:30:00Z
**Result:** `Phase 00: PASS`

---

## 1. Strict Gates (independently re-executed, all zero exits)

| Gate | Command | Exit |
|---|---|---|
| check | `cargo check --workspace --all-features` | 0 |
| fmt | `cargo fmt --all --check` | 0 |
| clippy | `cargo clippy --workspace --all-targets --all-features -- -D warnings` | 0 |
| test | `cargo test --workspace --all-features` | 0 |

Test summary: 2801 library tests passed (0 failed, 6 ignored); integration tests
including `input_integration_tests`, `sdl_driver_tests`, `sound_integration`,
`zip_integration_test` all pass; doc-tests 1 passed / 5 ignored.

The `-luqm_rust` linker blocker is resolved: `input_integration_tests` runs
and passes. No `ld: library 'uqm_rust' not found` error.

## 2. No Lint/Test Weakening

- No crate-level (`#![allow]`) or module-level blanket allows added.
- 187 item-level `#[allow]` added, all with explicit `reason` tied to C ABI
  compatibility during Rust migration. Categories: `non_snake_case` (99),
  `clashing_extern_declarations` (43), `clippy::not_unsafe_ptr_arg_deref` (27),
  `clippy::missing_safety_doc` (4), `clippy::too_many_arguments` (7 pre-existing).
- No `.rustfmt.toml` or `.clippy.toml` config changes.
- No tests removed (1 `#[test]` attribute removal was a merge of duplicate
  test function headers; 2 new tests added).
- No `RUST_AUTOMATION` Cargo feature added (REQ-BUILD-002).
- No automation module/options/hooks exist in source.

## 3. Preservation and Source Grounding

All required source facts independently verified:

- **DoInput pump order** (`gameinp.c:367+`): `TFB_ProcessEvents()` →
  `ProcessUtilityKeys()` → `TaskSwitch()` → `UpdateInputState()` → journal →
  sounds → `inputCallback()` → `InputFunc()`. Two pump opportunities precede
  injection point; one update precedes sound/callback/InputFunc.
- **Abort clear inventory** (5 sites): `restart.c:318`, `setupmenu.c:1229`,
  `battle.c:489`, `pickmele.c:385`, `fmv.c:109`.
- **Non-DoInput consumers**: `confirm.c::DoConfirmExit` (line 79+118),
  `talk_segue.rs::do_talk_segue -> c_UpdateInputState` (line 248).
- **Typed main-menu commit**: `do_restart.rs:274` does
  `state.cur_state = new_item.as_u8()`; `sync_cur_state` called at init
  (line 167). P06 must add sync at navigation commit point.
- **Present returns `()`**: `ffi.rs:626` `canvas.present()`.
- **Swap skip/no-redraw paths**: `dcqueue.c:636` `TFB_SwapBuffers(TFB_REDRAW_NO)`,
  `tfb_draw.c:219` `TFB_FlushGraphicsEx(TRUE)`.
- **Build capabilities**: `USE_RUST_THREADS`, `USE_RUST_GFX`, `USE_RUST_COMM`,
  `USE_RUST_RESTART` all `'1'` in `sc2/build.vars`; `RUST_OWNS_MAIN` in CFLAGS.
- **Binary-specific C link**: `build.rs:170`
  `cargo:rustc-link-arg-bin=uqm=-luqm_c`.

## 4. P00 Executable Probes (all passed, exit 0)

`bash probes/run_p00_probes.sh` — exit 0:

| Probe | Result |
|---|---|
| lock_free_atomics | AtomicU64(portable)=true, CAS all true |
| monotonic_instant | Monotonic confirmed, no regression |
| unix_datagram | path_len=82, sock_mode=600, nonce_ok, Darwin peer cred unsupported(EINVAL) |
| file_primitives | create_new_ok, rename_no_replace unsupported(Darwin), dir_sync=ok |
| process_identity | PID + lstart available, child PID distinct |
| sdl_dummy_hidden | driver=dummy, hidden=true, 320x240, pitch=1280 |

Archive: 328 members, sorted manifest, all 5 required members present
(`gameinp_rust_main.o`, `confirm.c.o`, `sdl_common.c.o`, `input.c.o`,
`dcqueue.c.o`).

## 5. P00 Linked Harness (all passed, exit 0)

`bash harness/run_p00_harness.sh` — exit 0:

- **7 production symbols** found in C archive via nm with correct source-grounded
  members: `DoInput`/`AnyButtonPress` → `gameinp_rust_main.o`;
  `DoConfirmExit` → `confirm.c.o`; `TFB_ProcessEvents`/`TFB_SwapBuffers` →
  `sdl_common.c.o`; `ProcessInputEvent` → `input.c.o`;
  `TFB_FlushGraphicsEx` → `dcqueue.c.o`.
- **Deterministic sorted manifest** (327 entries).
- **Force-load ordering** per §8.3: `-L$OUT_DIR` → `-force_load,shim.a` →
  `libuqm_c.a` → `libuqm_rust.a` → external libs → frameworks → compression.
- **All 6 mutation tests pass**: bypassing each symbol group reduces count
  correctly (mode 0=7, mode 1=5, mode 2=6, mode 3=5, mode 4=6, mode 5=6).
- **Link map** contains all 7 production symbols.
- **nm binary evidence** shows all 7 symbols in text section.
- Evidence saved at `/tmp/p00-harness-evidence/`.

## 6. SDL ABI/MUSTLOCK Real Surface Tests (all passed)

4 tests in `graphics::sdl_capture::tests`, all passed via `cargo test`:

- `test_create_and_query_mustlock_surface`: real MUSTLOCK surface (RLEACCEL),
  320x240, BPP=32, verified via C accessors compiled against linked SDL headers.
- `test_lock_copy_unlock_success`: real lock, write known data, copy via shared
  helper, verify bytes match.
- `test_injected_lock_failure_no_read`: inject lock failure, verify NO pixel
  data read (sentinel intact), helper returns error.
- `test_null_surface_returns_error`: null surface returns error.

Fault injection is thread-local (`_Thread_local`) so parallel tests cannot
contaminate each other.

## 7. Menu Binding Probe — Initialized-Child Production Query (exit 0)

`bash harness/run_menu_binding_probe.sh` — exit 0:

- **Build**: library built, all archives present (`libuqm_c.a`,
  `libp00_harness_shim.a`, `libuqm_rust.a`).
- **Symbol verification**: `VControl_ParseGesture` (rust_vcontrol_impl.c.o),
  `InstallGraphicResTypes` (resgfx.c.o), `InstallStringTableResType`
  (sresins.c.o), `InitResourceSystem`/`LoadResourceIndex`/`res_IsString`/
  `res_GetString`/`uio_openRepository`/`uio_mountDir`/`uio_openDir` (Rust),
  `uqm_query_menu_binding` (menu_binding_accessor.o) — all verified via nm.
- **Link**: force-load order per §8.3, link map preserved.
- **Execution**: probe IS the initialized child (owns/reaps no children).
  Production init: `InitResourceSystem` → `uio_openRepository` →
  `uio_mountDir` → `uio_openDir` → `LoadResourceIndex("menu.key","menu.")`
  → `uqm_query_menu_binding("down")`.
- **Result**: `found=1`, `key_code=1073741905` (SDLK_DOWN=0x40000051),
  `binding_id=1`, `num_alternates=6`, `binding_type=VCONTROL_KEY`,
  `RESULT=PASS`.
- **Production origin**: `sc2/content/menu.key` defines
  `down.1 = STRING:key Down` (6 alternates), parsed through production
  `VControl_ParseGesture` (C wrapper → Rust `rust_VControl_ParseGesture`).
- **nm binary evidence**: `_main`, `_uqm_query_menu_binding`,
  `_VControl_ParseGesture`, `_InitResourceSystem`, `_LoadResourceIndex`,
  `_res_IsString`, `_res_GetString`, `_rust_VControl_ParseGesture` all in text.
- Evidence saved at `/tmp/p00-menu-binding-evidence/`.

## 8. Plan Completeness Verification

All 12 required items verified present in plan documents:

1. Exact reducer timelines (P02: table, max=3 1/admit/2/admit/3/timeout).
2. Complete ABI shells/mirrors/two-phase commit (P04: shell model, mirrors,
   commit protocol).
3. Callback-specific trace integration (P06 input/menu, P07 present/capture,
   P05 lifecycle).
4. Separate inactive receipt (P05/P08: `inactive-teardown-complete.json`
   distinct from active).
5. Atomic capture generation (P02 arm/model, P07 generation integration).
6. ABI-authoritative SDL tests (P07 linked SDL/MUSTLOCK, P00 sdl_capture).
7. No double DCQ init (P07: TFB_InitGraphics once, never calls
   Init_DrawCommandQueue directly).
8. Synchronous menu Stop propagation (P06: Stop through handle_navigate →
   do_restart_frame → rust_do_restart_frame).
9. talk_segue inventory (P06: `do_talk_segue -> c_UpdateInputState`).
10. ChildSession (P08: complete lifecycle, fault tests).
11. Autonomous real proof (P08: main-menu-v1, watchdog-v1, inactive-smoke,
    hard-hang).
12. REQ-SEM-002 ownership in P08.

## 9. Deterministic Archive/Rerun Inputs

- Object paths sorted (`archive_inputs.sort_by_key`).
- Manifest written and verified sorted (`LC_ALL=C sort -c` passes).
- Rerun dependencies: `build.vars`, `config_unix.h`, each `.o` file,
  recompiled C sources, harness sources/headers, `build.rs`.
- Rust anchor: `cargo:rustc-link-arg-bin=uqm=-luqm_c` (binary-specific).
- Shim/archive/group/external-library ordering matches §8.3.
- Harness archives don't leak into other targets (object-only compilation +
  script-level linking, not global `rustc-link-arg`).

## 10. Ordinary Cargo Integration Linking

- `cargo test --workspace --all-features` exits 0.
- `input_integration_tests` runs successfully (no `-luqm_rust` error).
- Integration test imports the package library's public Rust FFI API directly.

## 11. Evidence Preservation

- Baseline: `/tmp/uqm-runtime-baseline-20260723-005857/` (HEAD, status, RED
  clippy log, SHA-256 before/after).
- Remediation: `/tmp/uqm-p00-remediation-20260723/` (archive members,
  manifest, clippy suggestions).
- Harness: `/tmp/p00-harness-evidence/` (link map, nm, manifest).
- Menu binding: `/tmp/p00-menu-binding-evidence/` (link map, nm, probe output).

---

**Conclusion:** Every requirement of P00a is met. All four strict gates pass
with zero exits. All checked-in P00 probes pass. The initialized-child
`menu.down.N` production query is executed and validated. No source fixes or
waivers were applied during verification. P01 may start.
