# Phase 09: Config API — Stub [OK]

## Phase ID
`PLAN-20260224-RES-SWAP.P09`

## Completion Date
2026-02-24

## What Was Done
- Created `rust/src/resource/config_api.rs` with:
  - `ResourceDesc` struct (Rust-internal, not `#[repr(C)]`)
  - `ResourceMap` type alias (`HashMap<String, ResourceDesc>`)
  - `put_string()`, `put_integer()`, `put_boolean()`, `put_color()` — create/update entries
  - `get_string()`, `get_integer()`, `get_boolean()`, `get_color()` — typed getters returning `Option`
  - `serialize_entry()` — serialize to `TYPE:value` format
  - `save_resource_index()` — generate file content with root filtering and prefix stripping
- Registered `config_api` module in `mod.rs`
- Color data_num format: `(r << 24) | (g << 16) | (b << 8) | a` (matching C)
- Color serialization delegates to `serialize_color()` from `resource_type.rs`

## Verification
```
cargo test --lib --all-features — 1234 passed, 0 failed, 4 ignored
```

## Plan Markers
- `@plan PLAN-20260224-RES-SWAP.P09`
- `@requirement REQ-RES-047-065`
