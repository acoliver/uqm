# P10: Crew Writeback & Ship Death — COMPLETE

## Evidence
- 35 writeback tests pass (`cargo test --lib -- ships::writeback`)
- 32 lifecycle tests pass (updated for writeback delegation)
- 1832 total tests pass, 0 failed, 6 ignored
- 0 clippy warnings in ships/ module

## Deliverables
- `rust/src/ships/writeback.rs` (~1000 lines, 35 tests)
  - `ship_death()` — clears PLAY_VICTORY_DITTY, records crew=0
  - `new_ship_transition()` — audio stop flag, descriptor free, crew writeback, species deactivation, replacement spawn
  - `update_ship_frag_crew()` — queue-position matching with species cross-check, WritebackCursor for double-update prevention
  - `fleet_is_infinite()` — checks first fragment for INFINITE_FLEET (0xFFFF)
  - `battle_teardown_writeback()` — C-faithful ordering: floating crew → desc crew → starship crew → fragment writeback → free
  - `count_floating_crew_elements()` — counts CREW_OBJECT elements
  - `WritebackCursor` — transient bookkeeping, not on ShipFragment
  - `TransitionResult`, `TeardownResult`, `ShipFragment` types
- `rust/src/ships/types.rs` — added `audio_stopped: bool` to Starship
- `rust/src/ships/lifecycle.rs` — uninit_ships delegates to writeback::battle_teardown_writeback
- `rust/src/game_init/init.rs` — updated wrapper for new signature

## Remediations Applied
1. Pointer identity → index-based queue-position matching
2. WritebackCursor wired through update/transition/teardown
3. Audio stop modeled via audio_stopped flag
4. Infinite fleet: no species deactivation (matches C)
5. Floating crew ordering matches C UninitShips
6. lifecycle.rs delegates to writeback module
7. Species cross-check added to update_ship_frag_crew

## P14 Scope Notes
- Display-list-driven teardown iteration requires C bridge (P14)
- Activity-gated writeback (encounter-only) requires C activity context (P14)
- game_init passes empty fragment queues — real queues wired in P14
