# Phase Completion Marker: P00a — Preflight Verification

## Phase ID
`PLAN-20260325-COMMPT3.P00a`

## Timestamp
2026-03-25T11:35:00 (approx)

---

## Section Results

### Toolchain Verification — PASS

```
cargo 1.92.0 (Homebrew)
rustc 1.92.0 (ded5c06cf 2025-12-08) (Homebrew)
clippy 0.1.92
```

- `cargo --version` [OK]
- `rustc --version` [OK]
- `cargo clippy --version` [OK]
- C build system: not exercised (build.sh smoke-test deferred; C compilation
  verified implicitly by prior commpt2 completion). Not a blocker.
- `USE_RUST_COMM=on/off` build modes: verified via existing passing CI + commpt2
  completion marker. Not re-built here; not a blocker for P00a.

---

### Dependency Verification — PASS

```
grep -E "parking_lot|serial_test" rust/Cargo.toml
parking_lot = "0.12"
serial_test = "3.0"
```

- `parking_lot` [OK] (version 0.12)
- `serial_test` [OK] (version 3.0)

---

### Type/Interface Verification

#### C-side bridges — PASS

| Item | Location | Result |
|------|----------|--------|
| `c_SetColorMap(void *colormap)` | `rust_comm.c:1184` | [OK] EXISTS |
| `c_PlayMusic(void *song, int looping, int priority)` | `rust_comm.c:1158` | [OK] EXISTS |
| `CommData.AlienColorMap` via `c_SetCommDataAlienColorMap` | `rust_comm.c:1607-1610` | [OK] EXISTS |
| `CommData.AlienSong` via `c_SetCommDataAlienSong` | `rust_comm.c:1613-1615` | [OK] EXISTS |
| `GetColorMapAddress()` | `sc2/src/libs/gfxlib.h:465` | [OK] EXISTS |
| `PlayMusic()` | `sc2/src/uqm/settings.h:32` | [OK] EXISTS |

Evidence:
```
rust_comm.c:1158: c_PlayMusic (void *song, int looping, int priority)
rust_comm.c:1184: c_SetColorMap (void *colormap)
rust_comm.c:1186:   SetColorMap (GetColorMapAddress ((COLORMAP)colormap));
rust_comm.c:1607: c_SetCommDataAlienColorMap (uintptr_t cmap)
rust_comm.c:1609:   CommData.AlienColorMap = (COLORMAP)cmap;
rust_comm.c:1613: c_SetCommDataAlienSong (uintptr_t song)
rust_comm.c:1615:   CommData.AlienSong = (MUSIC_REF)song;
gfxlib.h:465: extern COLORMAPPTR GetColorMapAddress (COLORMAP);
settings.h:32: extern void PlayMusic (MUSIC_REF MusicRef, BOOLEAN Continuous, BYTE Priority);
```

#### Rust-side functions — PASS

| Item | Location | Result |
|------|----------|--------|
| `set_colormap()` | `talk_segue.rs:997` | [OK] EXISTS |
| `play_alien_music()` | `talk_segue.rs:939` | [OK] EXISTS |
| `rust_DoCommunication()` | `ffi.rs:715` | [OK] EXISTS |
| `rust_ShowConversationSummary()` | `ffi.rs:860` | [OK] EXISTS |
| `do_communication()` | `talk_segue.rs:435` | [OK] EXISTS |
| `player_response_input()` | `talk_segue.rs:377` | [OK] EXISTS |
| `select_response()` | `talk_segue.rs:343` | [OK] EXISTS |
| `alien_talk_segue()` | `talk_segue.rs:303` | [OK] EXISTS |

#### C-side subtitle access — PASS

| Item | Location | Result |
|------|----------|--------|
| `SubtitleText` static var | `comm.c:103` | [OK] EXISTS |
| `clear_subtitles` static var | `comm.c:102` | [OK] EXISTS |
| `last_subtitle` static var | `comm.c:104` | [OK] EXISTS |
| `add_text()` static function | `comm.c:152` | [OK] EXISTS (static, accessible within comm.c) |
| `optSubtitles` | `comm.c:1650` (used within `#ifndef USE_RUST_COMM`) | [OK] EXISTS |
| `GetTrackSubtitle()` | `trackplayer.h:50` | [OK] EXISTS |
| `#ifdef USE_RUST_COMM` block at line 1715+ | `comm.c:1715-1752` | [OK] EXISTS |

Evidence:
```
comm.c:102: static BOOLEAN clear_subtitles;
comm.c:103: static TEXT SubtitleText;
comm.c:104: static const UNICODE *last_subtitle;
comm.c:152: add_text (int status, TEXT *pTextIn)
comm.c:1715: #ifdef USE_RUST_COMM
comm.c:1752: #endif /* USE_RUST_COMM — static comm variable accessors */
trackplayer.h:48: extern const UNICODE *GetTrackSubtitleText (SUBTITLE_REF SubRef);
trackplayer.h:50: extern const UNICODE *GetTrackSubtitle (void);
```

---

### Call-Path Feasibility — PASS (with note)

#### Colormap path
- Plan: `talk_segue.rs:set_colormap` → `extern "C" c_SetColorMapFromCommData`
  → `rust_comm.c:c_SetColorMapFromCommData` → `SetColorMap(GetColorMapAddress(CommData.AlienColorMap))`
- Status: **c_SetColorMapFromCommData does NOT yet exist** — this is a new bridge
  to be created by P03/P05. Current code calls `c_SetColorMap(null)` as a stub.
  All prerequisite pieces exist: `GetColorMapAddress` (gfxlib.h:465),
  `CommData.AlienColorMap` (rust_comm.c:1609). Path is feasible. [OK]

#### Music path
- Plan: `talk_segue.rs:play_alien_music` → `extern "C" c_PlayAlienMusic`
  → `rust_comm.c:c_PlayAlienMusic` → `PlayMusic(CommData.AlienSong, TRUE, 1)`
- Status: **c_PlayAlienMusic does NOT yet exist** — new bridge for P03/P05.
  Current code calls `c_PlayMusic(null, 1, 1)` as a stub.
  All prerequisite pieces exist: `PlayMusic` (settings.h:32),
  `CommData.AlienSong` (rust_comm.c:1615). Path is feasible. [OK]

#### Subtitle path — PASS
- `c_ClearSubtitles` [rust_comm.c:562] → `rust_ClearSubtitles()` [ffi.rs:825] [OK]
- `c_CheckSubtitles` [rust_comm.c:568] → `rust_CheckSubtitles()` [ffi.rs:833] [OK]
- `c_RedrawSubtitles` [rust_comm.c:574] → `rust_RedrawSubtitles()` [ffi.rs:845] [OK]
- Note: subtitle statics (`SubtitleText`, `clear_subtitles`, `last_subtitle`,
  `add_text()`) accessible within `comm.c` translation unit; bridge functions
  added at `#ifdef USE_RUST_COMM` block at line 1715. Already populated with
  `c_SetTalkingFinished`, `c_SetupSubtitleTextFromCommData`, `c_SetCurInputState`,
  `c_ClearPhraseBuf`. New subtitle accessors fit naturally here.

#### Lock discipline path — PASS
- `rust_DoCommunication` at ffi.rs:715 acquires `COMM_STATE.write()`, calls
  `do_communication`, handles callback with lock released [OK] (verified by reading
  ffi.rs:715–760 which implements the described discipline)

---

### Test Infrastructure Verification — PASS

```
cd rust && cargo test --lib -- comm
test result: ok. 268 passed; 0 failed; 0 ignored; 0 measured; 2215 filtered out; finished in 0.01s
```

- 268 comm tests [OK]
- 0 pre-existing failures [OK]

---

## Blocking Issues

**None.**

1. `add_text()` is static in `comm.c` — **confirmed not a blocker**: plan already
   accounts for this; new functions added to `#ifdef USE_RUST_COMM` block at
   line 1715 which has full access to all statics in the translation unit.

2. `GetColorMapAddress` is NOT a macro — it is an extern function in `gfxlib.h:465`.
   `c_SetColorMapFromCommData` (to be created in P03/P05) can call it directly
   from C. Not a blocker.

3. `c_PlayMusic` and `c_SetColorMap` in `talk_segue.rs` extern block — currently
   have no callers other than `play_alien_music()` and `set_colormap()` respectively.
   P03/P05 will replace these calls with new dedicated bridges (`c_PlayAlienMusic`,
   `c_SetColorMapFromCommData`). Existing declarations can be removed or left in
   place; no other callers confirmed.

4. `c_PlayAlienMusic` and `c_SetColorMapFromCommData` do not exist yet — **expected**,
   these are the implementation targets of P03/P05. All prerequisite C symbols
   (`PlayMusic`, `GetColorMapAddress`, `CommData.AlienSong`, `CommData.AlienColorMap`)
   are confirmed present.

---

## Gate Decision

**PASS — proceed to P01.**

All toolchain, dependency, type/interface, call-path, and test infrastructure
checks satisfied. No blockers identified. The two "new bridge" call paths
(`c_PlayAlienMusic`, `c_SetColorMapFromCommData`) are correctly absent at this
stage — they are implementation targets, not prerequisites.
