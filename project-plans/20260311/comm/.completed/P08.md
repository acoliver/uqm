# P08 Encounter Lifecycle — Complete
- encounter.rs: lifecycle orchestration with callback tracking
- CallbackTracker: enforces EC-REQ-009/010/015/016 ordering
  - Normal exit: init→post→uninit (exactly once each)
  - Abort exit: init→uninit (skip post)
  - Attack-without-hail: post→uninit (no init)
- EncounterResources: resource handle tracking for teardown
- EncounterState: active flag + callback tracker + resources
- CommState.encounter field with clear() integration
- FFI exports: BeginEncounter, EndEncounterNormal/Abort/Attack,
  IsEncounterActive
- 10 encounter tests (serial for global state safety)
- 228 comm tests pass, full C+Rust link succeeds
