# PLAN-20260314-THREADING.P01a Verification Report

**Timestamp:** 2026-03-16T16:45:00Z  
**Phase:** P01a (Analysis Verification)  
**Status:** COMPLETE  
**Gate Decision:** **PASS** — proceed to Phase 02

---

## Documents Reviewed

- `project-plans/20260311/threading/plan/01-analysis.md` (the analysis under verification)
- `project-plans/20260311/threading/.completed/P01.md` (the P01 completion report)
- `project-plans/20260311/threading/requirements.md`
- `project-plans/20260311/threading/specification.md`
- `rust/src/threading/mod.rs` (actual source — spot-checked lines 586-705, 920-1010)
- `sc2/src/libs/threads/rust_thrcommon.c` (actual source — full file, 432 lines)
- `sc2/src/libs/threads/rust_threads.h` (actual source — full file, 73 lines)
- `sc2/src/libs/threads/thrcommon.c` (searched for callers and reference implementation)
- `sc2/src/libs/task/tasklib.c` (searched for WaitThread/CreateThread usage)
- `sc2/src/uqm.c` (verified StartThread caller)

## Codebase Searches Performed

| Search | Scope | Matches |
|--------|-------|---------|
| `StartThread\s*\(` | `*.c` | 6 matches (5 in legacy thrcommon.c, 1 in uqm.c:454) |
| `WaitThread\s*\(` | `*.c` | 6 matches (thrcommon.c:147, thrcommon.c:285,287; sdlthreads.c:324; rust_thrcommon.c:209,262) |
| `WaitThread\s*\([^,]+,\s*&` | `*.c` | 0 matches — no non-NULL status callers |
| `SleepThreadUntil\s*\(` | `*.c` | 61 matches across 23 files |
| `FinishTask\|ConcludeTask\|WaitThread` in tasklib.c | single file | 3 matches (FinishTask:50, FinishTask:56, ConcludeTask:63 — no WaitThread) |
| `pendingBirth` in rust_thrcommon.c | single file | 0 matches (only in legacy thrcommon.c) |
| `REQ-` in analysis | single file | 0 matches — analysis does not use REQ-* IDs |

---

## Verification Checklist

### Requirements Traceability is Accurate

#### [OK] PASS — Analysis does NOT claim nonexistent REQ-* IDs from `requirements.md`

**Evidence:** Searched the analysis for `REQ-` — zero matches. The analysis uses quoted prose from `requirements.md` instead of numbered IDs. The requirements document itself uses no `REQ-*` numbering scheme, so this is the correct approach.

#### [OK] PASS — G1 is traced to actual `requirements.md` thread-result / `WaitThread` statements

**Evidence:** G1 analysis quotes:
- "When a thread entry function returns an integer status, the threading subsystem shall preserve that status..." — verified at `requirements.md:62`
- "both WaitThread status-pointer requirements" — verified at `requirements.md:63-65` (non-null status, null status, and failure cases)
- "two-value adapter ABI limitation note" — verified at `requirements.md:66` ("The adapter ABI uses a two-value return convention...")

All three traceability claims match actual requirements text.

#### [OK] PASS — G2 is traced to actual `requirements.md` `SleepThreadUntil` async-pumping statements

**Evidence:** G2 references "all three `SleepThreadUntil` async-pumping obligations." Verified at `requirements.md:192-194`:
1. "preserve the legacy behavior of servicing pending asynchronous engine work" (L192)
2. "wake in time to service that asynchronous work before continuing to wait" (L193)
3. "remain compatible with the legacy engine contract in which sleeping on the main thread also advances asynchronous processing obligations" (L194)

All three are present and match the analysis's traceability claim.

#### [OK] PASS — G3 is traced to non-joinable lifecycle cleanup requirements plus spec §2.5 reference-design note

**Evidence:** G3 analysis references:
- "non-joinable thread lifecycle cleanup obligations" — verified at `requirements.md:39-42,86-89`
- "`StartThread` must preserve distinction from joinable creation" — verified at `requirements.md:42`
- "spec §2.5 [Reference design] note" — verified in specification §2.5 which says `StartThread_Core` "should use `rust_thread_spawn_detached`"

The analysis correctly documents this as reference design tension (not normative failure), and concludes to keep the current approach with documentation. This is an appropriate resolution.

#### [OK] PASS — G4 is traced to actual `StartThread` failure contract and non-joinable no-leak requirement

**Evidence:** G4 analysis references:
- "`StartThread` failure contract paragraph" — verified at `requirements.md:51` (extensive failure contract)
- "non-joinable thread no-leak requirement" — verified at `requirements.md:89`
- "spec §2.5 detached-thread creation failure contract" — verified in specification §2.5

The analysis correctly identifies this as an unresolvable ABI-level blocker within the current plan scope and does not overclaim closure.

#### [OK] PASS — G5/G6/G7 are traced to applicable spec/requirements text or explicitly marked as plan-quality cleanup

**Evidence:**
- G5 (stale TODOs): appropriately categorized as cleanup without false traceability claims
- G6: references spec §2.4 text about lifecycle processing being C-owned — verified in spec
- G7: references recursive mutex behavior requirements at `requirements.md:105-107` and plain mutex unresolved blocker at `requirements.md:112-118`

**Minor note:** G6 quotes spec language about "pendingBirth/pendingDeath arrays" but `rust_thrcommon.c` only has `pendingDeath[]`. This is acceptable because the analysis is quoting the spec (which refers to the reference design pattern), not claiming `pendingBirth` exists in the Rust adapter.

### Data Flow Completeness

#### [OK] PASS — G1: Full path traced from C ThreadFunction return → Rust closure → JoinHandle → join → out_status → WaitThread *status

**Evidence:** The analysis documents both the current (broken) and required data flows in detail. Spot-checked against actual source:

- `mod.rs:949`: `let _ = unsafe { func(data) };` — confirmed discards result [OK]
- `mod.rs:940`: `-> Result<Thread<()>>` — confirmed returns unit type [OK]
- `mod.rs:998-1007`: `rust_thread_join(thread: *mut RustThread) -> c_int` — confirmed no out_status param [OK]
- `rust_threads.h:36`: `extern int rust_thread_join(RustThread* thread);` — confirmed no out_status [OK]
- `rust_thrcommon.c:218-220`: `*status = result` where result is join success, not thread exit value — confirmed [OK]

The required data flow correctly shows the fix path.

#### [OK] PASS — G2: Legacy async-pumping loop behavior documented with function references

**Evidence:** G2 documents the 5-step loop (Async_process → check time → query Async_timeBeforeNextMs → sleep min → repeat). Verified against legacy `thrcommon.c:335-362` — the actual legacy code matches the described loop structure. The analysis also references `Async_process()` and `Async_timeBeforeNextMs()` as C functions from `libs/async.h`, included at `rust_thrcommon.c:16` — confirmed at line 16 of actual source.

#### [OK] PASS — G3: Current lifecycle path showing why `StartThread_Core` still needs a native handle is documented

**Evidence:** The analysis traces the full dependency chain:
1. `RustThreadHelper` calls `FinishThread(thread)` — confirmed at `rust_thrcommon.c:115`
2. `FinishThread` enqueues in `pendingDeath[]` — confirmed at `rust_thrcommon.c:226-246`
3. `ProcessThreadLifecycles` calls `WaitThread(t, NULL)` — confirmed at `rust_thrcommon.c:262`
4. `WaitThread` requires `t->native != NULL` — confirmed at `rust_thrcommon.c:216`

This proves `StartThread_Core` cannot simply switch to `rust_thread_spawn_detached` without breaking the cleanup path.

#### [OK] PASS — G4: Analysis correctly states that `JoinHandle` drop is detach in Rust but does NOT overclaim detached-failure contract closure

**Evidence:** G4 explicitly states:
- "P07 must not claim detached-creation failure semantics are solved"
- "The detached-failure contract mismatch remains an acknowledged ABI/design blocker outside this plan's implementation scope"
- "Full parity requires a follow-up design decision with concrete ABI/ownership changes"

No overclaim detected.

#### [OK] PASS — Detached-thread creation failure mismatch is explicitly called out as unresolved under the current ABI

**Evidence:** G4 analysis states: "It cannot satisfy the normative detached-thread creation failure contract from spec §2.5 / requirements.md, because `rust_thread_spawn_detached` returns `void`." This is accurate — confirmed from `mod.rs:972-984` and `rust_threads.h:34`.

### Integration Points Verified

#### [OK] PASS — All major callers listed (9 integration points, corrected from original 7)

**Evidence:** The updated analysis lists:
1. DCQ (`dcqueue.c:55-60`) — NOT affected [OK]
2. TFB Draw (`tfb_draw.c:201-212`) — NOT affected [OK]
3. Task system (`tasklib.c:40-42`) — correctly notes "does NOT directly call WaitThread" [OK]
4. Audio stream (`stream.c:571-576`) — NOT affected [OK]
5. Mixer (`mixer.c:110-112`) — NOT affected [OK]
6. Callbacks (`callback.c:43-73`) — plain mutex blocker noted [OK]
7. Logging (`uqmlog.c:56-162`) — plain mutex blocker noted [OK]
8. Main entry (`sc2/src/uqm.c:454`) — only `StartThread(...)` caller [OK]
9. SleepThreadUntil callers — "~40+ call sites across gameplay, UI, and engine modules" [OK]

**Verified completeness:** Codebase search for `StartThread\(` found exactly one non-legacy-thrcommon caller (`uqm.c:454`) — listed. `WaitThread\(` found only `thrcommon.c:147` and `rust_thrcommon.c:262` — both mentioned in G1 callers. `SleepThreadUntil\(` found 61 matches — the "~40+ call sites" claim is conservative (actual unique source files: ~23). No callers appear to be missing.

#### [OK] PASS — Each caller assessed for impact from each gap

**Evidence:** Each of the 9 touchpoints has an explicit "NOT affected" / "affected by G2" / "plain-mutex blocker note" assessment. The task system was corrected to note it does NOT call WaitThread (verified — `tasklib.c` has zero `WaitThread` references).

#### [OK] PASS — No additional callers missed

**Evidence:** Cross-referenced all codebase search results against the integration list. All relevant callers are accounted for:
- No `WaitThread(&status)` callers with non-NULL status in repository (confirmed 0 matches)
- `StartThread` has one real caller: `uqm.c:454` (listed)
- `SleepThreadUntil` has ~61 call sites in ~23 files (listed as "~40+ call sites")

### "Old Code to Replace" List Complete

#### [OK] PASS — Each entry has: location, current code, target code

**Evidence:** The 10-row table in the analysis has Location, Current, and Target columns. All entries are specific and unambiguous. Verified key entries:

| Entry | Verified? |
|-------|-----------|
| `mod.rs:949` — `let _ = unsafe { func(data) }` → capture return | [OK] Actual line 949 matches |
| `mod.rs:936` — `-> Result<Thread<()>>` → `-> Result<Thread<c_int>>` | [OK] Actual line 940 matches (line number approximate but code matches) |
| `mod.rs:997-1007` — join signature change | [OK] Actual lines 997-1007 match |
| `rust_threads.h:36` — `int rust_thread_join(RustThread*)` | [OK] Actual line 36 matches |
| `rust_thrcommon.c:218-220` — status result change | [OK] Actual lines 218-220 match |
| `rust_thrcommon.c:192-200` — SleepThreadUntil single-shot → loop | [OK] Actual lines 192-200 match |
| `rust_thrcommon.c:362-364` — stale comment | [OK] Actual lines 362-364 match |
| `mod.rs:596,611,681,696` — stale TODOs | [OK] Confirmed at lines 596, 611, 681, 697 (one line off for hibernation) |

#### [OK] PASS — No ambiguous entries

All entries are specific enough to act on. The additional ABI touchpoint for `rust_thrcommon.c:45` (local `extern` declaration) is documented separately in the G1 section and is not ambiguous.

### P01 Corrections Assessment

The P01 completion report identified 6 recommended corrections. The analysis has been updated to address them:

1. **[OK] Fixed:** "No current real WaitThread(..., &status) callers" — analysis G1 now states: "No current repository callers pass a non-NULL status pointer to WaitThread."
2. **[OK] Fixed:** Local `extern rust_thread_join(...)` declaration added — analysis G1 now has the additional ABI touchpoint note at line 62.
3. **[OK] Fixed:** `uqm.c:454` added as integration touchpoint #8.
4. **[OK] Fixed:** SleepThreadUntil callers acknowledged as integration touchpoint #9 with "~40+ call sites."
5. **Minor residual:** G6 wording still quotes spec language about `pendingBirth/pendingDeath` — acceptable because it's a spec quote, not a source claim.
6. **[OK] Fixed:** Task system touchpoint corrected to "does NOT directly call WaitThread."

---

## Findings Summary

### Items Verified Correct
- All 7 gaps (G1-G7) are accurately characterized and grounded in actual source code
- Requirements traceability is accurate — no phantom REQ-* IDs, all quoted requirements exist
- Data flows are complete and verified against actual source
- Detached-thread failure contract correctly identified as unresolved
- Plain-mutex recursion blocker correctly preserved and not collapsed
- Integration touchpoints corrected and now complete
- "Old code to replace" table is specific and verified

### Minor Residual Issues (Non-Blocking)

1. **G6 spec quote mentions `pendingBirth`:** The analysis quotes spec §2.4 which references `pendingBirth/pendingDeath arrays`. The Rust adapter only has `pendingDeath[]`. This is acceptable because the analysis is quoting the spec text verbatim, not claiming the Rust adapter has `pendingBirth`. However, a parenthetical noting that only `pendingDeath` is present in `rust_thrcommon.c` would improve clarity in a future revision.

2. **SleepThreadUntil caller count:** The analysis says "~40+ call sites" but the actual count is 61 matches across ~23 source files. The undercount is conservative and not harmful, but could be updated for precision.

3. **Line number drift:** A few line numbers in the "Old code to replace" table are approximate (e.g., `mod.rs:936` vs actual `940`, `mod.rs:696` vs actual `697`). These are within the range expected for living source files and do not affect correctness.

---

## Gate Decision

### [OK] PASS — Proceed to Phase 02

**Rationale:** All checklist items pass verification. The analysis corrections from P01 are adequate — integration touchpoints are now complete, requirements traceability is accurate, data flows are verified against actual source, and the detached-thread failure contract is properly scoped as unresolved. The residual issues are minor documentation polish that do not block downstream phases.
