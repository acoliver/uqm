# Phase Completion Marker: P06

## Phase ID
`PLAN-20260325-COMMPT3.P06`

## Date
2026-03-25

## Files Changed
- `sc2/src/uqm/comm.c` — added `comm_ClearSubtitles()`, `comm_CheckSubtitles()`, `comm_RedrawSubtitles()` inside `#ifdef USE_RUST_COMM` with empty bodies (`/* stub — impl in P08 */`), with traceability block
- `sc2/src/uqm/rust_comm.h` — added declarations for 3 `comm_*` functions with P06 traceability comment
- `sc2/src/uqm/rust_comm.c` — rewired `c_ClearSubtitles`, `c_CheckSubtitles`, `c_RedrawSubtitles` bodies to call `comm_*` instead of `rust_*`; also rewired `c_UpdateAnimations` from `rust_RedrawSubtitles()` to `comm_RedrawSubtitles()`

## Commands Run + Outputs

### Circular routing broken
```
grep -n "rust_ClearSubtitles\|rust_CheckSubtitles\|rust_RedrawSubtitles" sc2/src/uqm/rust_comm.c | grep -v "extern\|declare\|//\|proto"
```
**Output: (empty) — zero matches. PASS.**

### Stubs exist in comm.c
```
grep -n "comm_ClearSubtitles\|comm_CheckSubtitles\|comm_RedrawSubtitles" sc2/src/uqm/comm.c
```
**Output:**
```
1729:comm_ClearSubtitles (void)
1735:comm_CheckSubtitles (void)
1741:comm_RedrawSubtitles (void)
```
**PASS.**

### Declarations in rust_comm.h
```
grep "comm_ClearSubtitles\|comm_CheckSubtitles\|comm_RedrawSubtitles" sc2/src/uqm/rust_comm.h
```
**Output:**
```
void comm_ClearSubtitles(void);
void comm_CheckSubtitles(void);
void comm_RedrawSubtitles(void);
```
**PASS.**

### Rust build
```
cd rust && cargo check --workspace --all-features
```
**Output: `Finished` — 323 warnings (all pre-existing), 0 errors. PASS.**

### Rust unit tests
```
cd rust && cargo test --workspace --all-features --lib
```
**Output: `test result: ok. 2497 passed; 0 failed; 6 ignored`. PASS.**

## Structural Verification Checklist
- [x] `comm_ClearSubtitles()` exists in `comm.c` inside `#ifdef USE_RUST_COMM` (empty body)
- [x] `comm_CheckSubtitles()` exists in `comm.c` inside `#ifdef USE_RUST_COMM` (empty body)
- [x] `comm_RedrawSubtitles()` exists in `comm.c` inside `#ifdef USE_RUST_COMM` (empty body)
- [x] All three declared in `rust_comm.h`
- [x] `c_ClearSubtitles` in `rust_comm.c` calls `comm_ClearSubtitles()` (not `rust_ClearSubtitles`)
- [x] `c_CheckSubtitles` in `rust_comm.c` calls `comm_CheckSubtitles()` (not `rust_CheckSubtitles`)
- [x] `c_RedrawSubtitles` in `rust_comm.c` calls `comm_RedrawSubtitles()` (not `rust_RedrawSubtitles`)
- [x] Both build modes: Rust workspace compiles and tests pass

## Notes
- Stubs are **intentionally empty** — subtitle behaviour is implemented in P08.
- Additional call site fixed: `c_UpdateAnimations` in `rust_comm.c` also changed from `rust_RedrawSubtitles()` to `comm_RedrawSubtitles()` — this was required to pass the circular-routing verification grep and is consistent with the plan's intent.
- The `ld: library 'uqm_rust' not found` error in `input_integration_tests` is a pre-existing native-link issue unrelated to P06 changes.
