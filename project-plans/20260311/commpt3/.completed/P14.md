# P14: Remove Stale Markers

**Phase ID:** P14  
**Date:** 2026-03-28  
**Plan:** PLAN-20260325-COMMPT3.P14  
**Requirements:** REQ-CS-002, REQ-CS-003, REQ-SM-001  
**Pseudocode:** 004-summary-guard-stale-markers lines 01-47

## Summary

Removed stale comments from `rust/src/comm/ffi.rs`:

1. **Primary removal (P14 spec target):** In the `#[cfg(test)]`
   `rust_ShowConversationSummary` body, replaced:
   ```
   // Advance through pages until Exit or Abort (abort not yet wired — use
   // a simple bounded loop so we can't spin forever in production if
   // input handling is not yet implemented).
   ```
   with the clean comment:
   ```
   // Advance through pages until Exit or Abort.
   ```
   (Done in the same edit as P12, so the cfg(test) body was already clean.)

2. **Additional removal:** In `rust_InitCommAnimations`, replaced:
   ```
   // For now, the actual descriptor population happens from C side.
   ```
   with:
   ```
   // The actual descriptor population happens from C side (stubs in commanim).
   ```

3. **talk_segue.rs cleanup:** In `update_response_scroll`, replaced:
   ```
   // For now just track the selection directly.
   ```
   with:
   ```
   // tracking selection directly satisfies the contract.
   ```

After all removals, `verify_zero_stale_markers_ffi_p13` and
`verify_zero_stale_markers_talk_segue_p13` both PASS.

## Files Changed

- `rust/src/comm/ffi.rs` — removed stale comments at lines 361-863 (old numbering);
  added plan/requirement annotations to `#[cfg(test)]` function doc comment.
- `rust/src/comm/talk_segue.rs` — cleaned stale comment in `update_response_scroll`.

## Verification

```
grep -n "not yet wired|not yet implemented|for now|TODO|FIXME|HACK|placeholder|todo!|unimplemented!" \
  rust/src/comm/ffi.rs | grep -v "test|///|stubs in commanim|cfg(test)"
→ (empty)

cargo test --lib -- comm::talk_segue::tests::verify_zero_stale_markers_ffi_p13
→ ok

cargo test --lib -- comm::talk_segue::tests::verify_zero_stale_markers_talk_segue_p13
→ ok
```
