# Phase 04: ShipBehavior Trait & Registry — COMPLETE

## Phase ID
`PLAN-20260314-SHIPS.P04`

## Completion Date
2026-03-19

## Deliverables
- `rust/src/ships/traits.rs` — Full ShipBehavior trait with 7 hooks + supporting types (ShipState, BattleContext, WeaponElement, CollisionHandler). 11 tests.
- `rust/src/ships/registry.rs` — Complete 28-species template table, TemplateOnlyShip (non-panicking), 4 public dispatch functions. 21 tests.
- `rust/src/ships/races/mod.rs` — Empty placeholder for P11-P13.
- `rust/src/ships/mod.rs` — Updated with traits, registry, races module declarations.
- `rust/src/ships/types.rs` — Re-exports ShipBehavior from traits.rs, added UnimplementedSpecies error variant.

## Test Results
- 72 ships tests pass (36 existing + 36 new)
- 0 clippy warnings in ships/ module

## Verification
- P04a verification: PASS
- Structural checklist: all items PASS
- Semantic checklist: all items PASS
- Spot-checked 7 species against C source: all correct
- Remediation: TemplateOnlyShip panic removed (stores template at construction time)
- Pre-existing workspace clippy issues noted but not P04-scoped

## Key Design Decisions
- TemplateOnlyShip stores pre-validated RaceDescTemplate (no panic in descriptor_template())
- All 28 species return TemplateOnlyShip for now (real behavior in P11-P13)
- Resource IDs all zero (actual loading in P05)
- create_metadata_only_desc() and create_race_desc() are identical at P04; diverge when combat behavior is added
