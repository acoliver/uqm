# P07: Queue & Build Primitives — COMPLETE

## Date: 2026-03-19

## Deliverables
- `rust/src/ships/queue.rs` — 20 public functions, 36 tests
- `rust/src/ships/mod.rs` — Added `pub mod queue;`
- `rust/src/ships/types.rs` — Added missing `captains_name_index` field to ShipFragment

## Functions Implemented
1. `build_ship()` — Creates zero-initialized Starship in queue
2. `get_starship_from_index()` / `_mut()` — Queue index lookup
3. `clone_ship_fragment()` — Fragment from fleet template (with Sa-Matra convention)
4. `add_escort_ships()` — Sorted insertion with gap-filling, Sa-Matra special case
5. `count_escort_ships()` / `have_escort_ship()` — Query helpers
6. `escort_feasibility_study()` — Available slot count
7. `set_race_allied()` — Alliance state (DeadGuy protection)
8. `check_alliance()` — Alliance query
9. `start_sphere_tracking()` — Returns Option<usize> matching C race/0 contract
10. `pick_captain_name()` / `name_captain()` — RNG-injected captain naming
11. `get_index_from_starship()` — Reverse lookup
12. `remove_some_escort_ships()` / `remove_escort_ships()` — Ship removal
13. `calculate_escorts_worth()` — Total fleet cost
14. `set_escort_crew_complement()` — Crew/captain update (crew_level comparison)
15. `get_escort_by_starship_index()` / `_mut()` — Fragment lookup by window index

## Remediations Applied (from verification)
1. Removed global `static SEED: AtomicU32` — RNG now injected via parameter
2. Changed `start_sphere_tracking()` from `bool` to `Option<usize>` (C parity)
3. Fixed `add_escort_ships()` insertion ordering (sorted by window index)
4. Added `get_escort_by_starship_index()` (missing from initial implementation)
5. Fixed `set_escort_crew_complement()` comparison: `crew_level` not `max_crew`
6. Added Sa-Matra captain naming special case (captain index = 0)

## Verification
- 36 queue tests pass
- 1721 total tests pass
- 0 clippy warnings in ships/
