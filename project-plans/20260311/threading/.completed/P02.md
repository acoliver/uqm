# PLAN-20260314-THREADING.P02 Completion Report

Timestamp: 2026-03-14T16:10:56Z
Phase: P02 (Pseudocode)
Status: COMPLETE

## Scope Executed

Verified the pseudocode plan at `project-plans/20260311/threading/plan/02-pseudocode.md` against:
1. Actual source code in:
   - `/Users/acoliver/projects/uqm/rust/src/threading/mod.rs`
   - `/Users/acoliver/projects/uqm/sc2/src/libs/threads/rust_thrcommon.c`
   - `/Users/acoliver/projects/uqm/sc2/src/libs/threads/rust_threads.h`
   - `/Users/acoliver/projects/uqm/sc2/src/libs/threads/thrcommon.c` (legacy reference)
2. P01 completion report at `project-plans/20260311/threading/.completed/P01.md`
3. All corrections and findings from P01 analysis

## Verification Summary

### Pseudocode Accuracy
[OK] **PASS** - All pseudocode accurately represents required changes

### Coverage of P01 Gaps
[OK] **PASS** - All gaps G1-G4 covered with algorithmic pseudocode
[OK] **PASS** - Gaps G5-G7 correctly identified as comment/documentation changes not needing pseudocode

### Verification Against Source Code

#### G1: Return Value Propagation
**Status:** Verified accurate

- **spawn_c_thread (lines 01-10):** Correctly shows change from `Thread<()>` to `Thread<c_int>` with return value capture
- **rust_thread_spawn (lines 11-18):** Correctly shows boxing of `Thread<c_int>` type
- **rust_thread_spawn_detached (lines 19-27):** Correctly shows detached pattern with spawn_c_thread
- **rust_thread_join (lines 28-41):** Correctly shows new signature with `out_status` parameter and separate join-success return value
- **WaitThread (lines 42-52):** Correctly shows distinction between join success (`result`) and thread exit status (`out_status`)
- **rust_threads.h declaration (line 53):** Correctly shows new FFI signature
- **rust_thrcommon.c local extern declaration (line 54):** [OK] **ADDED** - Addresses P01 correction about missing local extern declaration at line 45

Current source verification:
- `mod.rs:660-668`: Current `spawn_c_thread` returns `Thread<()>` and discards result with `let _ = unsafe { func(data) };`
- `rust_thrcommon.c:45`: Current local extern is `extern int rust_thread_join(RustThread* thread);`
- `rust_threads.h:36`: Current declaration is `extern int rust_thread_join(RustThread* thread);`
- `rust_thrcommon.c:218`: Current call is `int result = rust_thread_join(t->native);` without out_status

Pseudocode accurately captures all required changes.

#### G2: SleepThreadUntil Async Pumping
**Status:** Verified accurate against legacy implementation

- **SleepThreadUntil (lines 55-66):** Pseudocode matches legacy `thrcommon.c:335-362` algorithm exactly:
  - Infinite loop
  - `Async_process()` call
  - Time check for early return
  - `Async_timeBeforeNextMs()` query
  - Overflow-safe TimeCount conversion: `(nextTimeMs / 1000) * ONE_SECOND + ((nextTimeMs % 1000) * ONE_SECOND / 1000)`
  - Clamp to wakeTime
  - Sleep and loop

Current source (`rust_thrcommon.c:193-200`):
```c
void SleepThreadUntil (TimeCount wakeTime)
{
	TimeCount now = GetTimeCounter();
	if (wakeTime > now)
	{
		SleepThread(wakeTime - now);
	}
}
```

This is a single-shot sleep without async pumping. Pseudocode correctly captures required change to match legacy behavior.

#### G3: StartThread_Core
**Status:** Verified as documentation-only change

- **StartThread_Core (lines 67-79):** Pseudocode correctly documents current behavior and explains why `rust_thread_spawn` must remain (not switched to detached variant)
- Decision note accurately reflects that lifecycle cleanup path via `ProcessThreadLifecycles()` requires joinable handle

Current source (`rust_thrcommon.c:159-181`):
```c
void StartThread_Core (ThreadFunction func, void *data, SDWORD stackSize, const char *name)
{
	TrueThread thread;
	ThreadStartInfo *startInfo;

	(void)stackSize;
	thread = AllocThreadHandle (name);
	startInfo = (ThreadStartInfo *) HMalloc (sizeof (*startInfo));
	startInfo->func = func;
	startInfo->data = data;
	startInfo->thread = thread;

	thread->native = rust_thread_spawn (name, RustThreadHelper, startInfo);
	if (thread->native == NULL)
	{
		HFree (startInfo);
		HFree (thread);
	}
}
```

Pseudocode matches actual implementation and correctly explains the lifecycle dependency.

#### G4: Detached Helper Cleanup
**Status:** Verified as correctly documenting ABI limitation

- **rust_thread_spawn_detached (lines 80-93):** Pseudocode correctly shows pattern of calling spawn_c_thread and dropping handle
- Comment correctly documents unresolved ABI issue: failure signal cannot propagate back to C caller synchronously

Current source (`mod.rs:719-732`):
```rust
#[no_mangle]
pub unsafe extern "C" fn rust_thread_spawn_detached(
    name: *const c_char,
    func: unsafe extern "C" fn(*mut c_void) -> c_int,
    data: *mut c_void,
) {
    let name_str = if name.is_null() {
        None
    } else {
        CStr::from_ptr(name).to_str().ok()
    };

    let _ = spawn_c_thread(name_str, func, data);
}
```

Current implementation discards Result silently. Pseudocode correctly identifies this as insufficient for proper error handling but documents the limitation honestly rather than claiming to solve it.

### P01 Corrections Addressed

All corrections from P01 analysis have been incorporated:

1. [OK] Local extern declaration in `rust_thrcommon.c:45` added to change set (line 54 of pseudocode)
2. [OK] Pseudocode correctly distinguishes join-success return value from thread exit status in WaitThread
3. [OK] SleepThreadUntil pseudocode verified against actual legacy implementation in `thrcommon.c`
4. [OK] G3 decision correctly reflects lifecycle dependency on joinable handle
5. [OK] G4 honestly documents ABI limitation rather than claiming false resolution

### Pseudocode Line Numbering
All pseudocode lines are sequentially numbered 01-93 with proper traceability:
- Lines 01-54: G1 changes (return value propagation through all layers)
- Lines 55-66: G2 changes (async pumping loop)
- Lines 67-79: G3 documentation (lifecycle handle requirement)
- Lines 80-93: G4 documentation (detached cleanup limitation)

## Files Changed

Updated:
- `project-plans/20260311/threading/plan/02-pseudocode.md` - Added line 54 for local extern declaration correction

## Verification Commands Run

Read operations:
- `read_file` on `02-pseudocode.md`
- `read_file` on `P01.md` completion report
- `read_file` on `rust/src/threading/mod.rs`
- `read_file` on `sc2/src/libs/threads/rust_thrcommon.c`
- `read_file` on `sc2/src/libs/threads/rust_threads.h`
- `read_file` on `sc2/src/libs/threads/thrcommon.c` (legacy reference)
- `read_line_range` for specific function implementations
- `search_file_content` for "extern int rust_thread_join" in rust_thrcommon.c

## Outputs Summary

### Pseudocode Quality Assessment
- **Accuracy:** All pseudocode matches actual source code structure and required changes
- **Completeness:** All P01 gaps covered; all P01 corrections incorporated
- **Traceability:** Clear line numbering and traceability table maps pseudocode to gaps and implementation phases
- **Algorithmic Clarity:** Pseudocode uses clear imperative statements suitable for direct implementation

### Key Findings

1. **G1 Return Value Propagation:** Pseudocode correctly captures the full data flow from C thread function through Rust closure to join return, including both the join-success flag and the thread exit status via out-parameter.

2. **G2 SleepThreadUntil:** Pseudocode accurately reproduces the legacy async-pumping loop algorithm including the overflow-safe TimeCount conversion formula.

3. **G3 StartThread_Core:** Pseudocode documents (not implements) the decision to keep current behavior, with clear explanation of lifecycle dependency.

4. **G4 Detached Helper:** Pseudocode honestly documents the ABI limitation rather than claiming to solve it within Rust-only scope.

5. **Local Extern Declaration:** P01 finding about duplicate extern declaration in rust_thrcommon.c has been incorporated as line 54 of the pseudocode.

### No Implementation Performed
As instructed, no actual code changes were made to source files. Only the pseudocode plan was verified and corrected.

## Semantic Findings

### Verified Design Decisions

1. **Thread<c_int> vs Thread<()>:** The change from discarding thread return values to propagating them is necessary for correct WaitThread status reporting.

2. **Two-value return in rust_thread_join:** The design correctly separates "did the join succeed" (c_int return) from "what did the thread return" (out_status pointer). This matches the C error-handling pattern.

3. **Async pumping in SleepThreadUntil:** The pseudocode correctly implements the pattern from legacy code, which is necessary for proper async task processing during sleep periods.

4. **Lifecycle handle requirement:** The pseudocode correctly identifies that switching StartThread_Core to detached variant would break existing lifecycle cleanup mechanism.

### Verified Algorithmic Correctness

1. **Overflow-safe conversion in SleepThreadUntil:** The formula `(nextTimeMs / 1000) * ONE_SECOND + ((nextTimeMs % 1000) * ONE_SECOND / 1000)` correctly prevents overflow by splitting the multiplication into seconds and milliseconds portions.

2. **Early return optimization:** The `if wakeTime <= now: RETURN` check prevents unnecessary sleep calls.

3. **Clamping to wakeTime:** The `if wakeTime < nextTime: SET nextTime = wakeTime` ensures we don't sleep past the target wake time even if async has no pending work.

## Follow-up Notes

### Ready for Implementation
The pseudocode is complete, accurate, and ready for implementation phases P03-P07.

### Implementation Phase Mapping
Per the traceability table:
- **P03 (Stubs):** Lines 01-18, 28-41 (G1 Rust FFI signatures)
- **P05 (Implementation):** Lines 01-54 (full G1 implementation including C adapter)
- **P06 (Implementation):** Lines 55-66 (G2 SleepThreadUntil)
- **P07 (Implementation/Docs):** Lines 67-93 (G3 docs, G4 implementation with limitations documented)

### No Blocking Issues
All pseudocode has been verified against actual source code. No algorithmic errors, missing steps, or inconsistencies found.

### Completion Criteria Met
[OK] Pseudocode written for all code-change gaps (G1-G4)
[OK] Comment-only gaps (G5-G7) correctly excluded from pseudocode
[OK] All P01 corrections incorporated
[OK] Pseudocode verified against actual source files
[OK] Traceability table complete
[OK] No code implementation performed (verification-only phase)

---

Phase P02 (Pseudocode) is complete and verified.
Next phase: P03 (Stub Implementation)
