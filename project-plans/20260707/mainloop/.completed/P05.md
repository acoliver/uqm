# Phase P05 Complete: State Machine Logic

**Phase ID:** `PLAN-20260707-MAINLOOP.P05`
**Requirement:** REQ-ML-004 — Activity Dispatch & Loop Control
**Status:** ✅ COMPLETE — Implementation done, pending deepthinker verification (P05a)

## Summary

Implemented the activity state machine as pure, testable Rust functions
encoding the dispatch and loop-control logic from `Starcon2Main`
(`starcon.c:255-322`). Every function takes values as parameters (not C
globals), making them trivially unit-testable without FFI.

## Deliverables

- **`rust/src/mainloop/state_machine.rs`** (~675 lines) with:
  - `ActivityDecision` enum (5 variants: InstallBombAtEarth, VisitStarBase,
    RaceCommunication, ExploreSolarSystem, Battle)
  - `BreakAction` enum (4 variants: Continue, InitBlackUrqCommunication,
    ClearRestart, JustBreak)
  - `GameStateInfo` struct (5 fields for C global snapshots)
  - `should_zero_velocity(current, next)` — load-path predicate (line 260)
  - `resolve_load_activity(current, next)` — load-path activity replacement (line 262)
  - `evaluate(activity, state)` — 5-branch dispatch (lines 265-306)
  - `pre_dispatch_mutate(activity, decision)` — pre-dispatch activity mutation
  - `post_encounter_clear(activity)` — encounter-only flag clearing (lines 285-290)
  - `check_break(activity, state)` — break condition with side-effect enum (lines 311-320)
  - `should_continue(activity)` — do-while continuation (line 322)
- **`rust/src/mainloop/mod.rs`** — `pub mod state_machine;` + re-exports.

## C Reference Traceability

Each function's doc comment cites exact `starcon.c` line numbers and
includes the original C code in a fenced block.

## Test Results

```
running 29 tests
test result: ok. 29 passed; 0 failed; 0 ignored
```

Full crate: **2632 passed; 0 failed; 6 ignored**.

### Test coverage (29 tests across 7 functions):

| Function | Tests | Key cases |
|----------|-------|-----------|
| `should_zero_velocity` | 3 | No load → zero; current load → suppress; next load → suppress |
| `resolve_load_activity` | 2 | CHECK_LOAD → next; no load → current |
| `evaluate` | 7 | Bomb±starbase, global flags, encounter, interplanetary, battle, bomb priority |
| `pre_dispatch_mutate` | 4 | VisitStarBase/RaceComm set START_ENCOUNTER; Explore/Battle set kind |
| `post_encounter_clear` | 4 | Clears START_ENCOUNTER, sets START_INTERPLANETARY when kind matches, preserves on CHECK_ABORT/CHECK_LOAD |
| `check_break` | 7 | Continue, won, dead, Kohr-Ah BlackUrq, CHECK_RESTART clear, skipped on ABORT, skipped on LOAD |
| `should_continue` | 2 | No abort → continue; abort → stop |

## Checklist

- [x] All 7 functions match `starcon.c:255-322` exactly
- [x] Encounter block entered on `START_ENCOUNTER OR bomb_state==2` (line 265-266)
- [x] `post_encounter_clear` conditional on `!(CHECK_ABORT|CHECK_LOAD)` (line 285)
- [x] `post_encounter_clear` conditionally sets START_INTERPLANETARY when kind==IN_INTERPLANETARY (line 288-289)
- [x] `check_break` guards on `!(CHECK_ABORT|CHECK_LOAD)` (line 311)
- [x] `should_zero_velocity` checks `(current | next) & CHECK_LOAD` (line 260)
- [x] `resolve_load_activity` returns `next` when CHECK_LOAD (line 263)
- [x] Zero `todo!()`/`unimplemented!()`/`unwrap()` calls
- [x] Zero compiler warnings from state_machine.rs
- [x] Zero TODO/FIXME/HACK/placeholder markers
- [x] 29 Tier-1 tests pass
- [x] Full crate test suite passes (2632 tests)
- [x] Module declared and re-exported from mod.rs
