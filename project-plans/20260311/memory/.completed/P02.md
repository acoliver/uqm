# Phase P02 Verification — PLAN-20260314-MEMORY

## Source Documents Reviewed
- Pseudocode: `/Users/acoliver/projects/uqm/project-plans/20260311/memory/plan/02-pseudocode.md`
- Revised P01: `/Users/acoliver/projects/uqm/project-plans/20260311/memory/.completed/P01.md`
- Implementation: `/Users/acoliver/projects/uqm/rust/src/memory.rs`

## Summary
Final verdict: **IMPLEMENTABLE**

The pseudocode is well-formed, logically sound, and covers all gaps identified in the revised P01 analysis. All five original gaps plus the P01 revisions are addressed. The pseudocode maps cleanly to Rust implementations and follows existing project patterns.

---

## Coverage Verification

### Component 1: Zero-size OOM fix (Lines 01-40)

#### Gaps addressed
- **Gap 1** (from P01): Zero-size allocation OOM paths unchecked in `rust_hmalloc`, `rust_hcalloc`, `rust_hrealloc`
- **P01 Revision to Gap 1**: Added `REQ-MEM-ZERO-002` citation

#### Requirements coverage
- `REQ-MEM-OOM-001`: Detection and fatal abort on OOM
- `REQ-MEM-ZERO-001`: Zero-size normalization to 1 byte
- `REQ-MEM-ZERO-002`: No invalid operations on zero-size (specifically memset on null)
- `REQ-MEM-OOM-003`: OOM detection applies to 1-byte normalization allocations

#### Implementability verdict
**IMPLEMENTABLE**

#### Logic verification
- Lines 01-12 (`rust_hmalloc`): Zero-size path checks for null after `malloc(1)` and aborts with appropriate message. Positive-size path unchanged from current behavior.
- Lines 13-26 (`rust_hcalloc`): Zero-size path checks for null after `malloc(1)` before performing `memset(ptr, 0, 1)`. This prevents invalid memory operation on null pointer. Positive-size path unchanged.
- Lines 27-40 (`rust_hrealloc`): Zero-size path frees existing pointer if non-null, then checks new `malloc(1)` for null before returning. Positive-size path unchanged.

All three functions now consistently abort on OOM for zero-size normalization, matching the positive-size path behavior.

#### Mapping to existing code structure
- Current zero-size branches are at lines 10-12 (hmalloc), 42-46 (hcalloc), 64-69 (hrealloc)
- Pseudocode adds 4 lines to each zero-size branch for null check and abort
- Uses existing `log_add(Fatal, ...)` + `abort()` pattern from positive-size paths
- No new dependencies or architectural changes required

---

### Component 2: `copy_argv_to_c` deallocator fix (Lines 41-57)

#### Gaps addressed
- **Gap 2** (from P01): Wrong deallocator used for CString pointers
- **Additional Gap A** (from P01): Incomplete/misleading documentation for `copy_argv_to_c`

#### Requirements coverage
- `REQ-MEM-OWN-006`: Allocations from `CString::into_raw()` must be freed with `CString::from_raw()`
- `REQ-MEM-INT-008`: Memory safety at Rust/C boundary

#### Implementability verdict
**IMPLEMENTABLE**

#### Logic verification
- Lines 41-54: Pseudocode describes existing algorithm (unchanged)
- Lines 56-57: Critical documentation clarification added as comments

The pseudocode correctly identifies:
- `c_strings` (Vec of `*mut i8` from `CString::into_raw()`) must use `CString::from_raw()` for cleanup
- `array_ptr` (from `rust_hmalloc`) must use `rust_hfree()` for cleanup

#### Mapping to existing code structure
- Current implementation at lines 109-141
- Error-path cleanup at lines 122-129 currently uses `libc::free()` (wrong)
- Test cleanup at lines 269-279 currently uses `libc::free()` (wrong)
- Fix requires changing deallocator calls but no algorithm changes
- Documentation at lines 104-107 currently says "must be freed with HFree" (incomplete/misleading)

#### Note on unreachable branch
The pseudocode comment at line 47 correctly notes that `rust_hmalloc` aborts on OOM, making the `array_ptr.is_null()` check at line 122-129 unreachable. However, P02 properly keeps the deallocation fix in the pseudocode since:
1. The branch exists in source and should use correct cleanup if reachable
2. Future refactoring might make it reachable
3. The documentation fix applies to all callers regardless

---

### Component 3: Unit-test gap closure (Lines 58-66)

#### Gaps addressed
- **Gap 3** (from P01): Missing explicit unit-test coverage
- **P01 Revision to Gap 3**: Expanded to include third missing test for `HRealloc(ptr, 0)` dedicated coverage

#### Requirements coverage
- `REQ-MEM-OWN-003`: `HFree(NULL)` must be safe
- `REQ-MEM-ALLOC-010`: `HRealloc(NULL, size)` must behave as `HMalloc(size)`
- `REQ-MEM-ZERO-003`: `HRealloc(ptr, 0)` ownership transition and non-null return (cited in P01 revision)

#### Implementability verdict
**IMPLEMENTABLE**

#### Logic verification
- Lines 58-59: Test that `rust_hfree(null)` does not crash — straightforward assertion
- Lines 61-66: Test that `rust_hrealloc(null, 64)` returns non-null, can be written/read, and freed — covers malloc-equivalence contract

#### Coverage note
The pseudocode covers only 2 of the 3 missing tests identified in revised Gap 3. The third test obligation (`HRealloc(ptr, 0)` dedicated coverage) is **not explicitly shown** in Component 3.

However, reviewing Component 4 reveals it is covered there:
- Lines 103-108: `test "realloc_zero_from_live_pointer_via_exported_abi"` allocates 16 bytes, reallocates to 0, asserts non-null return

**Conclusion**: All three missing unit tests from revised Gap 3 are covered across Components 3 and 4.

#### Mapping to existing code structure
- Current tests at lines 145-283
- `test_hfree` does not exist; line 58-59 adds new test
- `test_hrealloc` exists (lines 196-214) but does not cover null-pointer input case; lines 61-66 add missing scenario
- Current `test_zero_size_allocations` (lines 216-234) checks `rust_hrealloc(ptr, 0)` returns non-null but does not explicitly verify old pointer freed or document the contract; lines 103-108 add dedicated coverage

---

### Component 4: Rust-side ABI integration tests (Lines 67-108)

#### Gaps addressed
- **Gap 4** (from P01): No project-level mixed-language integration tests
- **P01 Revision to Gap 3**: Third missing unit test (`HRealloc(ptr, 0)`) appears here as lines 103-108

#### Requirements coverage
- `REQ-MEM-INT-009`: Mixed-language integration testing
- `REQ-MEM-ZERO-003`: Realloc-to-zero behavior (via lines 103-108)

#### Implementability verdict
**IMPLEMENTABLE**

#### Logic verification
- Lines 67-72: Basic allocation/free round trip via exported ABI
- Lines 74-78: Zero-fill verification via exported ABI
- Lines 80-86: Realloc data preservation via exported ABI
- Lines 88-97: Zero-size normalization for all three functions via exported ABI
- Lines 99-101: Init/uninit lifecycle smoke test via exported ABI
- Lines 103-108: Dedicated realloc-to-zero behavior test (covers revised Gap 3)

All tests follow standard Rust test patterns: allocate → assert → write/read → verify → free.

#### Mapping to existing code structure
- Current tests at lines 145-283 are all in `mod tests` with `#[test]` attribute
- New tests use identical pattern and will fit in same module
- Tests verify ABI by calling the `pub unsafe extern "C" fn` functions directly
- No cross-language FFI harness needed for Rust-side ABI tests (they test the exported functions from Rust)

#### Note on "integration" vs "unit"
The P01 Gap 4 description emphasized **mixed-language** integration tests (C code calling Rust, Rust calling C, etc.). The pseudocode Component 4 tests are still **Rust-side** tests calling the exported ABI functions.

**This is acceptable** because:
1. The exported functions are the ABI contract surface
2. Testing them from Rust still exercises the `extern "C"` boundary
3. P01 specification §14.2 (lines 492-501) explicitly says "project test suite" integration tests, not necessarily C harness tests
4. True cross-language tests would require C test harness infrastructure not shown in reviewed plan sources

The pseudocode accurately describes these as "Rust-side ABI integration tests" (line 67 comment), distinguishing them from pure unit tests of internal helpers.

---

### Component 5: Traceability markers (Lines 109-115)

#### Gaps addressed
- **Gap 5** (from P01): Missing requirement traceability markers
- **P01 Revision to Gap 5**: Existing marker at line 18 is stale and references non-existent requirement

#### Requirements coverage
- General traceability discipline per plan standards

#### Implementability verdict
**IMPLEMENTABLE**

#### Logic verification
- Lines 109-111: Add `@plan PLAN-20260314-MEMORY.P05` and systematic `@requirement REQ-MEM-*` markers to all exported functions
- Lines 113-115: Add `@plan PLAN-20260314-MEMORY.P04` and local requirement markers to `copy_argv_to_c`

The pseudocode correctly distinguishes:
- Exported ABI functions get phase P05 markers (implementation phase)
- Internal helper `copy_argv_to_c` gets phase P04 markers (different responsibility scope)

#### Mapping to existing code structure
- Current marker at line 18 references `PLAN-20260224-MEM-SWAP.P05` and `REQ-MEM-005` (stale)
- Fix requires replacing stale marker and adding markers to 6 other exported functions (hmalloc, hfree, hcalloc, hrealloc, mem_init, mem_uninit)
- Also add marker to `copy_argv_to_c` (line ~109 in current source)

#### Note on requirement mapping
The pseudocode says "add systematic @requirement REQ-MEM-* markers matching actual contract" (line 111) but does not specify which requirements map to which functions.

**This is acceptable** because:
1. P01 already verified that all relevant requirements exist in `requirements.md`
2. The implementing phase (P05) will need to do detailed requirement mapping
3. P02 pseudocode phase is concerned with **structure** of traceability, not exhaustive enumeration

For completeness, the implementing phase should map:
- `rust_hmalloc`: REQ-MEM-ALLOC-001, REQ-MEM-ZERO-001, REQ-MEM-OOM-001, REQ-MEM-OOM-003
- `rust_hcalloc`: REQ-MEM-ALLOC-002, REQ-MEM-ZERO-001, REQ-MEM-ZERO-002, REQ-MEM-OOM-001, REQ-MEM-OOM-003
- `rust_hrealloc`: REQ-MEM-ALLOC-003, REQ-MEM-ZERO-001, REQ-MEM-ZERO-003, REQ-MEM-OOM-001, REQ-MEM-OOM-003, REQ-MEM-OWN-002
- `rust_hfree`: REQ-MEM-ALLOC-004, REQ-MEM-OWN-003
- `rust_mem_init`: REQ-MEM-INT-001
- `rust_mem_uninit`: REQ-MEM-INT-002
- `copy_argv_to_c`: REQ-MEM-OWN-006, REQ-MEM-INT-008

---

## Gap Coverage Summary

### Original 5 Gaps (from P01)
| Gap | Pseudocode Component | Lines | Status |
|-----|---------------------|-------|--------|
| Gap 1: Zero-size OOM unchecked | Component 1 | 01-40 | [OK] Covered |
| Gap 2: Wrong deallocator | Component 2 | 41-57 | [OK] Covered |
| Gap 3: Missing unit tests | Components 3 & 4 | 58-66, 103-108 | [OK] Covered |
| Gap 4: No integration tests | Component 4 | 67-108 | [OK] Covered |
| Gap 5: Missing traceability | Component 5 | 109-115 | [OK] Covered |

### P01 Revisions
| Revision | Pseudocode Component | Lines | Status |
|----------|---------------------|-------|--------|
| Gap 1: Add REQ-MEM-ZERO-002 | Component 1 | 13-26 (hcalloc) | [OK] Covered (null check before memset) |
| Gap 3: Add HRealloc(ptr,0) test | Component 4 | 103-108 | [OK] Covered |
| Gap 3: Add REQ-MEM-ZERO-003 | Component 4 | 103-108 | [OK] Covered |
| Gap 5: Note stale marker | Component 5 | 109-111 | [OK] Covered (systematic replacement) |
| Additional Gap A: copy_argv_to_c docs | Component 2 | 56-57 | [OK] Covered |

---

## Logic Error Analysis

### Component 1 Logic Review
[OK] **No logic errors detected**

Verification:
- Zero-size branches correctly check `malloc(1)` return before using pointer
- Positive-size branches unchanged (already correct)
- All abort paths use identical pattern: `log_add(Fatal, <message>)` then `abort()`
- `rust_hrealloc` correctly frees old pointer before attempting new 1-byte allocation

### Component 2 Logic Review
[OK] **No logic errors detected**

Verification:
- Pseudocode correctly describes current algorithm (no changes to allocation logic)
- Documentation clarification at lines 56-57 is accurate per Rust CString ownership rules
- No code changes proposed, only cleanup path fixes (addressed in implementation, not pseudocode)

### Component 3 Logic Review
[OK] **No logic errors detected**

Verification:
- Line 58-59: `rust_hfree(null)` test is trivial and safe (current implementation already checks `!ptr.is_null()` at line 30)
- Lines 61-66: `rust_hrealloc(null, 64)` test correctly follows malloc contract (C standard: realloc(NULL, size) ≡ malloc(size))
- Pattern matches existing test structure

### Component 4 Logic Review
[OK] **No logic errors detected**

Verification:
- Lines 67-72: Basic allocate/write/verify/free — standard pattern
- Lines 74-78: Zero-fill verification — correct for calloc contract
- Lines 80-86: Realloc data preservation — standard pattern, correctly checks first 32 bytes after realloc to 256
- Lines 88-97: Zero-size for all three functions — correctly verifies non-null return
- Lines 99-101: Init/uninit smoke — correctly calls both functions and checks bool return
- Lines 103-108: Realloc-to-zero — correctly allocates 16 bytes, reallocates to 0, asserts non-null, frees result

All tests use `ASSERT` correctly and follow safe cleanup patterns.

### Component 5 Logic Review
[OK] **No logic errors detected**

Verification:
- Marker format `@plan PLAN-ID` and `@requirement REQ-ID` matches existing pattern at line 18
- Systematic application to all exported functions is correct
- Distinction between P05 (exported ABI) and P04 (internal helper) markers is reasonable

---

## Implementability Assessment by Component

### Component 1: Zero-size OOM fix
- **Effort**: Low (4 lines per function × 3 functions = ~12 lines added)
- **Risk**: Low (well-understood pattern, exact copy of existing positive-size null checks)
- **Dependencies**: None (uses existing logging and abort infrastructure)
- **Blockers**: None

### Component 2: `copy_argv_to_c` deallocator fix
- **Effort**: Low (~6 lines changed in error path, ~4 lines in test cleanup, ~3 lines docs)
- **Risk**: Low (CString::from_raw is well-documented Rust standard pattern)
- **Dependencies**: None (std::ffi::CString already in use)
- **Blockers**: None

### Component 3: Unit-test gap closure
- **Effort**: Low (~8 lines total for 2 tests)
- **Risk**: Low (trivial test cases)
- **Dependencies**: None (existing test infrastructure)
- **Blockers**: None

### Component 4: Rust-side ABI integration tests
- **Effort**: Medium (~60 lines total for 6 tests)
- **Risk**: Low (follows existing test patterns exactly)
- **Dependencies**: None (existing test infrastructure)
- **Blockers**: None

### Component 5: Traceability markers
- **Effort**: Low (~1-2 lines per function × 7 functions = ~10-14 lines)
- **Risk**: None (documentation only)
- **Dependencies**: None
- **Blockers**: Requires requirement mapping decisions (but mappings are straightforward per P01)

---

## Overall Implementability

### Total Effort Estimate
- Component 1: ~12 lines code
- Component 2: ~13 lines code/docs
- Component 3: ~8 lines code
- Component 4: ~60 lines code
- Component 5: ~10-14 lines docs

**Total: ~103-107 lines of code/docs added or modified**

Given existing file is 283 lines, this represents **~35-38% touched lines** but nearly all changes are localized additions rather than refactoring.

### Risk Assessment
- **No high-risk changes**: All modifications are additions or targeted fixes
- **No architectural changes**: Existing API surface and structure unchanged
- **No new dependencies**: All pseudocode uses existing imports and patterns
- **Well-tested pattern reuse**: OOM abort pattern already proven in positive-size paths

### Blockers
**None identified**

All pseudocode components:
- Use existing dependencies and imports
- Follow established patterns in the codebase
- Require no new build system configuration
- Require no cross-module coordination

---

## Adjustments Needed for P01 Revisions

### Adjustment 1: REQ-MEM-ZERO-002 coverage
**Status**: Already covered in Component 1, lines 13-26

The pseudocode null-check before `memset(ptr, 0, 1)` at line 19 explicitly prevents the invalid memory operation identified in P01 revision to Gap 1.

**No adjustment needed.**

### Adjustment 2: Third unit test (HRealloc(ptr, 0))
**Status**: Already covered in Component 4, lines 103-108

The pseudocode includes `test "realloc_zero_from_live_pointer_via_exported_abi"` which provides the missing dedicated coverage identified in P01 revision to Gap 3.

**No adjustment needed.**

### Adjustment 3: REQ-MEM-ZERO-003 citation
**Status**: Implicitly covered in Component 4, lines 103-108

The test at lines 103-108 validates the realloc-to-zero contract (old pointer freed, non-null return). Component 5 traceability markers should include this requirement for `rust_hrealloc`.

**Minor documentation adjustment needed**: When implementing Component 5, ensure `rust_hrealloc` traceability includes `REQ-MEM-ZERO-003`.

### Adjustment 4: Stale marker replacement
**Status**: Addressed in Component 5, lines 109-111

The pseudocode says "add @plan PLAN-20260314-MEMORY.P05 marker" which implicitly replaces the existing stale `PLAN-20260224-MEM-SWAP.P05` marker at line 18.

**Clarification recommended**: Component 5 should explicitly say "replace existing stale marker" rather than just "add marker" to make the revision action clear.

### Adjustment 5: Additional Gap A (copy_argv_to_c docs)
**Status**: Addressed in Component 2, lines 56-57

The pseudocode adds explicit documentation clarifying that:
- `c_strings` must use `CString::from_raw()`
- `array_ptr` must use `rust_hfree()`

This directly addresses the incomplete/misleading documentation identified in P01 Additional Gap A.

**No adjustment needed.**

---

## Final Verdict

### IMPLEMENTABLE

#### Reasoning
1. [OK] All 5 original gaps from P01 are covered
2. [OK] All P01 revisions are covered (REQ-MEM-ZERO-002, third unit test, REQ-MEM-ZERO-003, stale marker, Additional Gap A)
3. [OK] No logic errors detected in any pseudocode component
4. [OK] All components use existing patterns and dependencies
5. [OK] No architectural changes or refactoring required
6. [OK] Low total effort (~103-107 lines) and low risk
7. [OK] No blockers identified

#### Minor Recommendations for Implementation Phase
1. When implementing Component 5, explicitly note "replace stale marker at line 18" in addition to systematic additions
2. When implementing Component 5 traceability for `rust_hrealloc`, ensure `REQ-MEM-ZERO-003` is included in the marker list
3. Consider adding a brief comment in the realloc-to-zero test (lines 103-108) noting it validates ownership transition per REQ-MEM-ZERO-003

These are documentation clarity recommendations, not implementability blockers.

---

## Gap-to-Pseudocode Mapping Table

| Gap ID | Gap Description | Pseudocode Component | Lines | Coverage |
|--------|----------------|---------------------|-------|----------|
| Gap 1 | Zero-size OOM unchecked | Component 1 | 01-40 | Complete: adds null checks to all three zero-size branches |
| Gap 1 (revised) | + REQ-MEM-ZERO-002 | Component 1 | 13-26 | Complete: null check before memset in hcalloc |
| Gap 2 | Wrong deallocator | Component 2 | 41-57 | Complete: documents correct CString::from_raw() vs rust_hfree() usage |
| Gap 3a | Missing HFree(NULL) test | Component 3 | 58-59 | Complete: adds null_free_is_safe test |
| Gap 3b | Missing HRealloc(NULL,size) test | Component 3 | 61-66 | Complete: adds realloc_null_ptr_acts_as_malloc test |
| Gap 3c (revised) | Missing HRealloc(ptr,0) test | Component 4 | 103-108 | Complete: adds realloc_zero_from_live_pointer test |
| Gap 3 (revised) | + REQ-MEM-ZERO-003 | Component 4 | 103-108 | Complete: test validates requirement contract |
| Gap 4 | No integration tests | Component 4 | 67-108 | Complete: adds 6 Rust-side ABI integration tests |
| Gap 5 | Missing traceability | Component 5 | 109-115 | Complete: systematic markers for all functions |
| Gap 5 (revised) | Stale marker at line 18 | Component 5 | 109-111 | Complete: systematic replacement includes stale marker |
| Additional Gap A | copy_argv_to_c docs | Component 2 | 56-57 | Complete: clarifies cleanup responsibilities |

**Total coverage: 11/11 gap items (5 original + 6 revisions/additions) = 100%**
