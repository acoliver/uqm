# P10: C Wiring + Build Integration — PASS

## Changes

### C Wiring (restart.c)
- Added `#include "rust_bridge_restart.h"` and `extern BOOLEAN rust_start_game(void);` under `#ifdef USE_RUST_RESTART`
- Wrapped `StartGame()` body with `#ifdef USE_RUST_RESTART / return rust_start_game(); / #else ... #endif`
- `DrawRestartMenuGraphic` and `DrawRestartMenu` already de-staticized (P04)

### Build System
- `build.vars.in`: Added USE_RUST_RESTART, uqm_USE_RUST_RESTART, SYMBOL_USE_RUST_RESTART_DEF, uqm_SYMBOL_USE_RUST_RESTART_DEF, and export statements
- `build/unix/build.config`: Added USE_RUST_RESTART to:
  - Disabled section (USE_RUST_RESTART=, SYMBOL_DEF undef, export)
  - Enabled section (USE_RUST_RESTART=1, SYMBOL_DEF define, CCOMMONFLAGS -DUSE_RUST_RESTART, export)
- `build.vars`: Added USE_RUST_RESTART='1' entries
- `config_unix.h`: Added `#define USE_RUST_RESTART`
- `Makeinfo`: rust_bridge_restart.c in CFILES, .h in HFILES

### C Bridge (rust_bridge_restart.c/.h)
- Added MENU_STATE lifecycle functions: uqm_create_menu_state, uqm_set_menu_priv_data, uqm_destroy_menu_state

## Verification
- Rust release build: PASS (zero errors)
- Clean C rebuild (rm -rf obj/ && ./build.sh uqm): PASS (zero errors)
- Binary smoke test (./uqm --help): PASS
- rust_start_game symbol present in binary (nm confirms)
- Binary: 13.8MB arm64 Mach-O

VERDICT: PASS
