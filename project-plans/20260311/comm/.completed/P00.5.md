# P00.5 Preflight Verification — PASS

## Toolchain
- cargo 1.92.0, rustc 1.92.0, clippy 0.1.92 (>= 1.75 requirement met)
- parking_lot 0.12, serial_test 3.0, thiserror 1.0 all present

## Type Verification
- COMM_STATE: LazyLock<RwLock<CommState>> confirmed
- ResponseEntry.response_func: Option<usize> (needs change to fn ptr — G1/G22)
- TrackManager: no callback/completion model (confirms G5/G6)
- AnimContext: no ANIMATION_DESC/BlockMask (confirms G14)
- C LOCDATA in comm.h, CONVERSATION enum in commglue.h
- RESPONSE_FUNC: void (*)(RESPONSE_REF), RESPONSE_REF: COUNT(unsigned)
- ANIMATION_DESC in commanim.h, MAX_ANIMATIONS=20
- USE_RUST_COMM defined in config_unix.h:87
- Trackplayer: C-owned, splice_track exists in Rust FFI, no CommitTrackAdvancement/PollPendingTrackCompletion

## Call-Path Feasibility
- init_race(CONVERSATION) → init_*_comm() → LOCDATA* (27 race functions declared)
- RaceCommunication() at comm.c:1446, called from starcon.c:260
- NPCPhrase(index) → NPCPhrase_cb(index, NULL) — needs Rust impl
- PHRASE_ENABLED/DISABLE_PHRASE use string table NUL-prefix — needs Rust alternative

## PS-REQ-007 Audit: PASS
- 25/27 scripts use DISABLE_PHRASE (329 usages)
- 326 PHRASE_ENABLED guard checks
- Pattern is universally: if (PHRASE_ENABLED(x)) { NPCPhrase(x); DISABLE_PHRASE(x); }
- No script calls NPCPhrase on a previously disabled phrase
- myconc.c RAMBLE_* false positive verified (RAMBLE phrases never disabled)

## Build System
- Full C+Rust link succeeds (commit 27fe8e6bb)
- comm.c:400 guards only init/uninit — rest of comm.c/commglue.c/commanim.c unguarded (confirms G19)
- 2390 tests pass, 181 comm-specific
