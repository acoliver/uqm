# P11: Race Batch 1 — Simple Ships (8 Races) — COMPLETE

## Evidence
- 24 race tests pass (`cargo test --lib -- ships::races`)
- 302 total ships tests pass
- 1856 total tests pass, 0 failed, 6 ignored
- 0 clippy warnings in ships/ module
- All 20 descriptor fields match C source for all 8 races (verified by deepthinker)

## Deliverables
- `rust/src/ships/races/arilou.rs` — ArilouShip (auto-aim laser + teleport)
- `rust/src/ships/races/human.rs` — HumanShip (nuke + point-defense)
- `rust/src/ships/races/spathi.rs` — SpathiShip (torpedo + BUTT missile)
- `rust/src/ships/races/supox.rs` — SupoxShip (gob + lateral thrust)
- `rust/src/ships/races/thraddash.rs` — ThraddashShip (ion blast + afterburner)
- `rust/src/ships/races/yehat.rs` — YehatShip (twin pulse + shield, returns 2 weapons)
- `rust/src/ships/races/druuge.rs` — DruugeShip (mass driver + furnace)
- `rust/src/ships/races/ilwrath.rs` — IlwrathShip (hellfire + cloak)
- `rust/src/ships/races/mod.rs` — Declares and re-exports all 8
- `rust/src/ships/registry.rs` — Dispatches to real impls for 8 races

## Verified Data Accuracy
All descriptor template fields match C #defines for all 8 races:
ship_flags, ship_cost, crew/max_crew, energy/max_energy, max_thrust,
thrust_increment, energy_regen, weapon_cost, special_cost, energy_wait,
turn_wait, thrust_wait, weapon_wait, special_wait, ship_mass,
fleet strength, fleet known_loc, intel weapon_range.

## P14 Scope Notes
- Complex weapon behaviors (tracking, collision, recoil) require C Element/display-list wiring
- Facing-dependent offsets (Yehat twin cannon geometry) need C SINE/COSINE integration
- Special abilities (teleport, shield, cloak, afterburner) need C state machine integration
