# P12: Summary Guard Stub — Bifurcate rust_ShowConversationSummary

**Phase ID:** P12  
**Date:** 2026-03-28  
**Plan:** PLAN-20260325-COMMPT3.P12  
**Requirements:** REQ-CS-002  
**Pseudocode:** 004-summary-guard-stale-markers lines 01-24

## Summary

Bifurcated `rust_ShowConversationSummary` in `rust/src/comm/ffi.rs` into two
cfg-gated variants:

- `#[cfg(not(test))]`: delegates directly to `c_SelectConversationSummary()`
  from the C runtime, returning 1.
- `#[cfg(test)]`: retains the Rust SummaryView page-advance loop for unit tests.

Also made `c_bridge` in `talk_segue.rs` `pub(super)` so the production path
can reach `super::talk_segue::c_bridge::c_SelectConversationSummary`.

## Files Changed

- `rust/src/comm/ffi.rs` — replaced single `rust_ShowConversationSummary` with
  two cfg-gated versions; added plan/requirement/pseudocode annotations.
- `rust/src/comm/talk_segue.rs` — changed `mod c_bridge` to `pub(super) mod c_bridge`
  (line 43) so sibling module `ffi.rs` can access the extern declaration.

## Verification

```
cargo check -p uqm           → 0 errors
cargo test --lib -- comm      → 290 passed, 0 failed
```
