# P14: C-Side Bridge Wiring — COMPLETE

## Evidence
- 1916 tests pass (`cargo test --lib`), 0 failed, 6 ignored
- Zero clippy warnings in ships/ffi.rs
- Format check passes
- P14a verification: PASS (after one remediation round)

## Deliverables

### Rust Side
- `rust/src/ships/ffi.rs` — FFI export surface with extern "C" functions:
  - Catalog: `rust_ships_load_catalog()`, `rust_ships_free_catalog()`, `rust_ships_get_cost_by_index()`
  - Loader: `rust_ships_load()`, `rust_ships_free()`
  - Queue/Build: `rust_ships_build()` (stub), `rust_ships_clone_fragment()` (direct copy)
  - Spawn/Lifecycle: `rust_ships_spawn()`, `rust_ships_init()`, `rust_ships_uninit()` (stub)
  - Runtime Callbacks: `rust_ships_preprocess()`, `rust_ships_postprocess()`, `rust_ships_death()` (stubs)
  - All functions use catch_unwind for panic safety, validate pointers
- `rust/src/ships/mod.rs` — Added `pub mod ffi;`
- `rust/src/game_init/ffi.rs` — Delegates ship FFI to `ships::ffi` (no duplicate surface)

### C Side
- `sc2/config_unix.h` — Added `#define USE_RUST_SHIPS`
- `sc2/build/unix/build.config` — Full USE_RUST_SHIPS toggle (SYMBOL, HAVE_SYMBOLS, export, CCOMMONFLAGS)
- `sc2/src/uqm/init.c` — Guards on `InitShips()`, `UninitShips()`
- `sc2/src/uqm/ship.c` — Guards on `ship_preprocess()`, `ship_postprocess()`, `spawn_ship()` + extern for death
- `sc2/src/uqm/loadship.c` — Guards on `load_ship()`, `free_ship()`
- `sc2/src/uqm/master.c` — Guards on `LoadMasterShipList()`, `FreeMasterShipList()`, `GetShipCostFromIndex()`
- `sc2/src/uqm/build.c` — Guard on `Build()`, TODO for CloneShipFragment

## Remediation (round 1)
1. Added `extern void rust_ships_death` to ship.c
2. Fixed status_flags copying in rust_ships_spawn (was empty, now copies from C)
3. Documented CloneShipFragment FFI signature mismatch in build.c
