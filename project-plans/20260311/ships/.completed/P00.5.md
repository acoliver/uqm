# P00.5 Preflight Verification — PASS

Completed: 2026-03-18

## Toolchain [OK]
- Rust 1.92.0 (exceeds 1.75+ req)
- Clippy 0.1.92
- Cargo 1.92.0

## Dependencies [OK]
- parking_lot 0.12
- serial_test 3.0
- bindgen 0.70
- No ships-specific feature flags needed initially

## Types/Interfaces [OK]

### C Side — races.h
- SPECIES_ID: 28 species (NO_ID through UR_QUAN_PROBE_ID), LAST_MELEE_ID = MMRNMHRM_ID, NUM_SPECIES_ID
- Capability flags: 11 flags (SEEKING_WEAPON through SHIELD_DEFENSE)
- Status flags: LEFT/RIGHT/THRUST/WEAPON/SPECIAL + speed flags
- RACE_DESC: ship_info, fleet, characteristics, ship_data, cyborg_control, uninit_func, preprocess_func, postprocess_func, init_weapon_func, data, CodeRef
- STARSHIP: full struct with RaceDescPtr, crew, icons, counters, flags, hShip, ShipFacing, playerNr, control
- SHIP_FRAGMENT: race_id, index, crew_level, max_crew, energy/max_energy, race_strings, icons, melee_icon
- FLEET_INFO: SpeciesID, allied_state, growth/crew/strength fields, locations, func_index

### C Side — Battle Engine
- ELEMENT: preprocess/postprocess/collision/death callbacks (element.h:109-112)
- GetElementStarShip/SetElementStarShip: macros on pParent (element.h:192-193)
- spawn_ship: ship.c:380
- InitShips/UninitShips: init.c/init.h
- AllocElement: element.h:207, PutElement/InsertElement: element.h:209-210
- TrackShip, SetVelocityVector: weapon.h, velocity.h
- initialize_laser, initialize_missile: weapon.h:51-52
- LoadGraphic = LoadGraphicInstance (nameref.h:25), LoadStringTable = LoadStringTableInstance (nameref.h:28)

### Rust Side — Existing Stubs
- game_init/init.rs: placeholder init_ships()/uninit_ships() — not connected to real ships
- game_init/master.rs: placeholder MasterShipList with hardcoded test data — not connected
- game_init/ffi.rs: exports rust_init_ships, rust_uninit_ships, rust_load_master_ship_list, rust_free_master_ship_list — NO C callers exist
- lib.rs: no `ships` module — confirmed

## Call Path Feasibility [OK]
- Master Catalog: C → LoadMasterShipList → load_ship(FALSE) → CODERES → init_race(). Clear Rust equivalent.
- Battle Spawn: C → spawn_ship → load_ship(TRUE) → element creation. AllocElement/PutElement accessible.
- Per-Frame Pipeline: C battle loop → ship_preprocess/postprocess. Rust can provide C-callable function pointers.
- Ship Death: free_ship → uninit_func. Rust cleanup achievable via trait or explicit uninit.

## Build Integration Pattern [OK]
- 21 USE_RUST_* flags already in build.vars with established pattern
- Pattern: uqm_USE_RUST_XXX + USE_RUST_XXX + SYMBOL_USE_RUST_XXX_DEF
- C files use `#ifdef USE_RUST_XXX` guards

## Test Baseline [OK]
- 1570 pass, 1 intermittent (passes isolated), 6 ignored
- serial_test available for FFI global-state tests

## Blocking Issues: NONE
- All C APIs accessible via extern "C" bindings
- Resource loading functions available
- Element creation/management APIs available
- USE_RUST pattern well-established

## Gate Decision: **PASS** — proceed to P01 Analysis
