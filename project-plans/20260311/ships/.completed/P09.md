# P09: Ship Spawn & Lifecycle — COMPLETE

## Date: 2026-03-19

## Deliverables
- `rust/src/ships/lifecycle.rs` — ~910 lines, 32 tests
- `rust/src/ships/mod.rs` — Added `pub mod lifecycle;`
- `rust/src/game_init/init.rs` — Delegates to ships::lifecycle

## Components Implemented

### Constants
- NUM_SIDES (2), NUM_PLAYERS (2), RPG_PLAYER_NUM (0), NPC_PLAYER_NUM (1)
- IN_ENCOUNTER (2), IN_LAST_BATTLE (3), SUPER_MELEE (1), CREW_OBJECT

### BattleState (global, Mutex-protected)
- space_init_count (ref-counted), ships_initialized flag
- Asset handle tracking (explosion, blast, asteroid, stars_in_space)

### Core Functions
1. spawn_ship() — Load descriptor, patch crew, configure element, idempotent
   - SIS crew_level=0 special case: leaves descriptor default (C: ship.c lines 398-402)
   - Crew clamped to max_crew (C: ship.c line 406-407)
2. init_space() — Ref-counted space asset init
3. uninit_space() — Ref-counted space asset teardown
4. init_ships() — Battle init, returns NUM_SIDES or 1 (hyperspace)
5. uninit_ships() — Floating crew retrieval (clamped to max), crew writeback, descriptor free
6. get_next_starship() — First unspawned alive ship in queue
7. get_initial_starships() — Spawns one per side (reverse order for non-SUPER_MELEE)

### Result Types
- SpawnResult (Spawned | AlreadySpawned)
- UninitResult { crew_retrieved: u16 }
- ElementConfig (data for P14 element allocation)

### Boundary Ownership Split (documented in module doc)
- Moved to Rust: space ref-count, asset tracking, spawn sequence, teardown, ship selection
- Remains in C: display list, galaxy, graphics context, planet/asteroid spawning, element allocation, activity state, Super Melee UI, player order

## Remediations Applied
1. Added SIS crew_level=0 special case handling
2. Removed all TODO/placeholder/stub markers
3. Added floating crew retrieval with survivor identification and max_crew clamping
4. Fixed game_init/init.rs docs to be explicit about P14 queue access
5. Added 5 new tests (SIS zero-crew, floating crew add/clamp, no survivor, dead ship)

## Verification
- 32 lifecycle tests pass
- 1797 total tests pass
- 0 clippy warnings in ships/
- 0 deferred-implementation markers
