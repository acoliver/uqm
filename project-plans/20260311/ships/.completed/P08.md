# P08: Shared Runtime Pipeline — COMPLETE

## Date: 2026-03-19

## Deliverables
- `rust/src/ships/runtime.rs` — ~1400 lines, 44 tests
- `rust/src/ships/mod.rs` — Added `pub mod runtime;`

## Components Implemented

### Constants & Helpers
- NUM_FACINGS (16), FULL_CIRCLE (64), HALF_CIRCLE, QUADRANT, OCTANT
- VELOCITY_SHIFT (5), ONE_SHIFT (2), CIRCLE_SHIFT (6), FACING_SHIFT (4)
- Element state flags: APPEARING, CHANGING, COLLISION_FLAG, FINITE_LIFE, PLAYER_SHIP, etc.
- normalize_facing(), facing_to_angle(), angle_to_facing(), display_to_world(), etc.
- COMPUTER_CONTROL constant for AI detection

### Trigonometry System
- cosine()/sine() with quarter-circle lookup table (16 entries, 4 quadrant mirrors)
- arctan() with 33-entry table and quadrant adjustment
- Matches C mathlib functional behavior

### VelocityState (Bresenham fixed-point physics)
- Mirrors C VELOCITY_DESC: vector/fract/error/incr packed fields
- get_current_components(), set_vector(), set_components(), delta_components()
- velocity_squared(), is_zero(), zero()
- Matches C velocity.c sign handling and MAKE_WORD packing

### ElementState
- Rust-side view of C ELEMENT fields for runtime processing
- hit_points() accessor (C union alias for crew_level)

### Pipeline Functions
1. ship_preprocess() — 6-step pipeline matching C ship_preprocess:
   - Input normalization, APPEARING setup, AI hook (COMPUTER_CONTROL), energy regen, race preprocess, turn, thrust
2. ship_postprocess() — 4-step pipeline matching C ship_postprocess:
   - Dead exit, weapon fire, special cooldown, race postprocess
3. inertial_thrust() — 4-case physics matching C exactly
4. delta_energy() — C DeltaEnergy with LOW_ON_ENERGY flag management
5. animation_preprocess() — Frame cycling with turn_wait
6. default_ship_collision() — Planet damage (hit_points >> 2, min 1), COLLISION_FLAG

### CollisionResult enum
- NoEffect, Damage, CrewPickup, Bounce, PlanetCollision

## Remediations Applied (from verification)
1. Fixed build_ship_state() unwrap() → Result<ShipState, ShipsError>
2. Added AI intelligence() hook for COMPUTER_CONTROL ships in preprocess
3. Fixed collision to use hit_points() (C union with crew_level)
4. Added hit_points() accessor on ElementState
5. Fixed dead clippy warning (unused cur_status_flags assignment)
6. Added 5 additional tests (AI hook, hit_points, collision parity)

## Verification
- 44 runtime tests pass
- 1765 total tests pass
- 0 clippy warnings in ships/
