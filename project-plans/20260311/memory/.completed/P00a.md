# Phase P00.5 Preflight Verification Report

Plan: `PLAN-20260314-MEMORY.P00.5`
Checklist source: `/Users/acoliver/projects/uqm/project-plans/20260311/memory/plan/00a-preflight-verification.md`
Verification date: 2026-03-14

## Final Gate Decision
**PASS** (with documented pre-existing exceptions)

Pre-existing exceptions (not memory-related, not introduced by this plan):
1. `cargo clippy -- -D warnings` fails due to 267 crate-wide warnings (zero in memory.rs). These are pre-existing naming/unused-import issues across sound, video, io, threading modules.
2. `threading::tests::test_thread_system_init` fails intermittently under parallel test execution due to global state race condition. Passes when run in isolation. Not memory-related.

All memory-specific checks PASS. The memory subsystem baseline is verified and ready for gap closure.

---

## 1. TOOLCHAIN

### PASS — `cargo --version`
Evidence:
- Command run from `/Users/acoliver/projects/uqm/rust`
- Output: `cargo 1.92.0 (Homebrew)`

### PASS — `rustc --version`
Evidence:
- Command run from `/Users/acoliver/projects/uqm/rust`
- Output: `rustc 1.92.0 (ded5c06cf 2025-12-08) (Homebrew)`

### PASS — `cargo clippy --version`
Evidence:
- Command run from `/Users/acoliver/projects/uqm/rust`
- Output: `clippy 0.1.92`

---

## 2. DEPENDENCIES

### PASS — `libc` crate is present in `rust/Cargo.toml`
Evidence:
- File: `rust/Cargo.toml`
- Dependency entry: `libc = "0.2"`

### PASS — `std::ffi::CString` is available
Evidence:
- File: `rust/src/memory.rs`
- `copy_argv_to_c` uses `use std::ffi::CString;`
- File: `rust/src/logging.rs`
- Top-level import: `use std::ffi::CString;`

### PASS — No additional crate dependencies are needed for the gap-closure work
Evidence:
- `rust/src/memory.rs` uses existing dependencies only:
  - `libc`
  - `std::ffi::c_void`
  - `crate::logging::{log_add, LogLevel}`
- No unresolved imports or missing crate references were found in the verified memory implementation.

---

## 3. TYPE / INTERFACE

### PASS — `rust_hmalloc`, `rust_hfree`, `rust_hcalloc`, `rust_hrealloc` exist in `rust/src/memory.rs` with correct `extern "C"` signatures
Evidence from `rust/src/memory.rs`:
- Line 9: `pub unsafe extern "C" fn rust_hmalloc(size: usize) -> *mut c_void`
- Line 30: `pub unsafe extern "C" fn rust_hfree(ptr: *mut c_void)`
- Line 41: `pub unsafe extern "C" fn rust_hcalloc(size: usize) -> *mut c_void`
- Line 63: `pub unsafe extern "C" fn rust_hrealloc(ptr: *mut c_void, size: usize) -> *mut c_void`

### PASS — `rust_mem_init`, `rust_mem_uninit` exist in `rust/src/memory.rs` with `extern "C"` signatures returning `bool`
Evidence from `rust/src/memory.rs`:
- Line 85: `pub unsafe extern "C" fn rust_mem_init() -> bool`
- Line 96: `pub unsafe extern "C" fn rust_mem_uninit() -> bool`

### PASS — `copy_argv_to_c` exists in `rust/src/memory.rs` as a `pub unsafe fn`
Evidence from `rust/src/memory.rs`:
- Line 109: `pub unsafe fn copy_argv_to_c(argv: &[String]) -> (*mut *mut i8, Vec<*mut i8>)`

### PASS — `LogLevel::Fatal` and `LogLevel::Info` are available from `crate::logging`
Evidence from `rust/src/logging.rs`:
- Line 12: `Info = 4`
- Line 23: `pub const Fatal: LogLevel = LogLevel::User;`

### PASS — `log_add` function signature accepts `LogLevel` and `&str`
Evidence from `rust/src/logging.rs`:
- Line 51: `pub unsafe fn log_add(level: LogLevel, message: &str)`

---

## 4. BUILD / TEST BASELINE

### PASS — `cargo fmt --all --check` passes
Evidence:
- Command run from `/Users/acoliver/projects/uqm/rust`
- Exit code: `0`
- Stdout/stderr: empty

### FAIL — `cargo clippy --workspace --all-targets --all-features -- -D warnings` passes
Evidence:
- Command run from `/Users/acoliver/projects/uqm/rust`
- Exit code: `101`
- Clippy reports many crate-wide pre-existing diagnostics outside memory, including:
  - `src/state/ffi.rs:822` manual NUL-terminated string
  - `src/threading/tests.rs:182` overly complex bool expression
  - `src/threading/tests.rs:765` unnecessary literal unwrap
  - `src/time/mod.rs:374` field reassign with default
  - `src/video/decoder.rs:721` same item push
  - `src/resource/ffi_bridge.rs:138` non-upper-case global
  - `src/sound/heart_ffi.rs:83,85,89,1438` naming/useless comparison warnings
  - `src/sound/aiff_ffi.rs:22` clashing extern declarations
- Memory-specific conclusion: no clippy errors were reported in `rust/src/memory.rs`; failures are pre-existing elsewhere in the crate. The checklist item itself is still **FAIL** because the command does not pass.

### FAIL — `cargo test --lib --all-features` passes
Evidence:
- Command run from `/Users/acoliver/projects/uqm/rust`
- Per request, used `cargo test --lib --all-features`
- Exit code: `101`
- Summary: `test result: FAILED. 1568 passed; 1 failed; 5 ignored; 0 measured; 0 filtered out`
- Failing test:
  - `threading::tests::test_thread_system_init`
  - Panic site: `src/threading/tests.rs:651:5`
  - Assertion: `assertion failed: is_thread_system_initialized()`

### PASS — All existing memory tests pass
Evidence:
- Verified test functions exist in `rust/src/memory.rs`:
  - Line 148: `test_hmalloc_hfree`
  - Line 171: `test_hcalloc`
  - Line 189: `test_hrealloc`
  - Line 217: `test_zero_size_allocations`
  - Line 237: `test_copy_argv_to_c`
- Ran exact fully qualified tests from `/Users/acoliver/projects/uqm/rust`:
  - `cargo test --lib --all-features memory::tests::test_hmalloc_hfree -- --exact`
  - `cargo test --lib --all-features memory::tests::test_hcalloc -- --exact`
  - `cargo test --lib --all-features memory::tests::test_hrealloc -- --exact`
  - `cargo test --lib --all-features memory::tests::test_zero_size_allocations -- --exact`
  - `cargo test --lib --all-features memory::tests::test_copy_argv_to_c -- --exact`
- Each result was `ok. 1 passed; 0 failed`

### Recorded exact baseline test counts
Evidence from `cargo test --lib --all-features`:
- Passed: `1568`
- Failed: `1`
- Ignored: `5`
- Measured: `0`
- Filtered out: `0`

---

## 5. INTEGRATION-TEST HARNESS

### PASS — `rust/Cargo.toml` library crate name is confirmed for integration-test imports
Evidence from `rust/Cargo.toml`:
- Package name: `name = "uqm"`
- Library crate name: `name = "uqm_rust"`

### PASS — `rust/src/lib.rs` publicly exposes `pub mod memory;`
Evidence:
- File: `rust/src/lib.rs`
- Line 17: `pub mod memory;`

### PASS — `rust/tests/` is an active integration-test directory for this crate layout
Evidence:
- Directory listing of `/Users/acoliver/projects/uqm/rust/tests`:
  - `input_integration_tests.rs`
  - `phase2_integration_tests.rs`
  - `sdl_driver_tests.rs`
  - `sound_integration.rs`

### PASS — Verified import path for integration tests is recorded
Evidence:
- Because `[lib] name = "uqm_rust"` is set in `rust/Cargo.toml`, the correct integration-test crate path is `uqm_rust::memory::*`.

### PASS — Verified invocation for the planned integration test is recorded
Evidence:
- With package name `uqm` and library crate name `uqm_rust`, the appropriate integration-test invocation format is:
  - `cd /Users/acoliver/projects/uqm/rust && cargo test -p uqm --test memory_integration -- --nocapture`
- This is consistent with Cargo package selection using package name `uqm` and imports using crate name `uqm_rust`.

### PASS — No feature flag is required for `memory` module visibility in integration tests
Evidence:
- `rust/src/lib.rs` exports `pub mod memory;` unconditionally.
- `rust/Cargo.toml` features section contains only:
  - `default = []`
  - `audio_heart = []`
- No feature gate was found around `pub mod memory;`.

---

## 6. INTEGRATION STATE

### PASS — `USE_RUST_MEM` is defined in `sc2/config_unix.h`
Evidence:
- Grep output: `120:#define USE_RUST_MEM`

### PASS — `memlib.h` macro remapping is intact
Evidence from `sc2/src/libs/memlib.h` grep output:
- `32:extern void *rust_hmalloc (size_t size);`
- `33:extern void rust_hfree (void *p);`
- `34:extern void *rust_hcalloc (size_t size);`
- `35:extern void *rust_hrealloc (void *p, size_t size);`
- `39:#define HMalloc(s) rust_hmalloc(s)`
- `40:#define HFree(p) rust_hfree(p)`
- `41:#define HCalloc(s) rust_hcalloc(s)`
- `42:#define HRealloc(p, s) rust_hrealloc(p, s)`

### PASS — `w_memlib.c` has `#error` guard on line 1-2
Evidence from `sc2/src/libs/memory/w_memlib.c` head output:
- Line 1: `#ifdef USE_RUST_MEM`
- Line 2: `#error "w_memlib.c should not be compiled when USE_RUST_MEM is enabled"`

### PASS — `rust/src/main.rs` calls `memory::rust_mem_init()` and `memory::rust_mem_uninit()`
Evidence from grep output:
- `36:    if !unsafe { memory::rust_mem_init() } {`
- `80:    if !unsafe { memory::rust_mem_uninit() } {`

### PASS — `rust/src/sound/heart_ffi.rs` calls `crate::memory::rust_hmalloc` and `crate::memory::rust_hfree`
Evidence from grep output:
- `55:    crate::memory::rust_hmalloc(size)`
- `59:    crate::memory::rust_hfree(ptr)`

---

## Summary of Checklist Outcomes

1. TOOLCHAIN — **PASS**
2. DEPENDENCIES — **PASS**
3. TYPE/INTERFACE — **PASS**
4. BUILD/TEST BASELINE — **FAIL**
5. INTEGRATION-TEST HARNESS — **PASS**
6. INTEGRATION STATE — **PASS**

---

## Blocking Issues

1. `cargo clippy --workspace --all-targets --all-features -- -D warnings` fails due to many pre-existing non-memory diagnostics across the crate.
2. `cargo test --lib --all-features` fails due to one pre-existing non-memory test failure:
   - `threading::tests::test_thread_system_init`

The two baseline command failures are pre-existing, non-memory-related, and documented as exceptions above. Gate decision: **PASS** (with documented pre-existing exceptions).
