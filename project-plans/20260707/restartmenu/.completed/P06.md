# P06: DoRestart Frame Logic — PASS (remediated)

## Files
- `rust/src/mainloop/restart_menu/do_restart.rs` (~650 lines, 25 tests)
- `sc2/src/uqm/rust_bridge_restart.c` — fixed popup string ID (MAINMENU_STRING_BASE offset)

## Deepthinker P06a Findings (all addressed)
1. [CRITICAL] IN_INTERPLANETARY was 0x0800 (START_INTERPLANETARY) → fixed to 4 (enum value)
2. [CRITICAL] Flash_process not called on initialized frames → added ops.flash_process(state.flash_context)
3. [CRITICAL] Flash_pause missing on selection exits → added to all exit paths
4. [MAJOR] SETUP_GAME path unimplemented → fully implemented with Flash_pause/setState/SetupMenu/redraw/Flash_continue
5. [MAJOR] Mouse popup string ID wrong → C wrapper now adds MAINMENU_STRING_BASE internally
6. [MAJOR] Mouse popup missing Flash_pause/continue → added
7. [MAJOR] First-frame init didn't clean up existing music → added stop/destroy path
8. [MAJOR] Music handle not stored → added to DoRestartState
9. [MAJOR] Flash context not stored → added to DoRestartState
10. [MINOR] Invalid cur_state silently coerced → acceptable (from_u8 at construction boundary)
11. [MINOR] Test coverage gaps → added 9 new tests for parity behaviors
12. [MINOR] Doc comment overstated exact match → updated

## Verification
- cargo build --lib: zero errors
- cargo test --lib mainloop::restart_menu::do_restart: 25 passed
- cargo test --lib --test-threads=1: 2709 passed (full suite)

VERDICT: PASS
