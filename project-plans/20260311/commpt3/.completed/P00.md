# Phase Completion Marker: P00 — Requirements Lock

## Phase ID
`PLAN-20260325-COMMPT3.P00`

## Timestamp
`2026-03-27T20:51:55Z`

---

## Validation Command Outputs

### CM (ColorMap) — `grep -n "null_mut" rust/src/comm/talk_segue.rs | grep -v test`
```
945:        c_bridge::c_PlayMusic(std::ptr::null_mut(), 1, 1);
1003:        c_bridge::c_SetColorMap(std::ptr::null_mut());
```
**CONFIRMED**: Line 1003 passes null to `c_SetColorMap` with "pass null for now" comment.

### MU (Music) — same command
**CONFIRMED**: Line 945 passes null to `c_PlayMusic`.

### SD (Subtitle) — `grep -n "rust_ClearSubtitles|rust_CheckSubtitles|rust_RedrawSubtitles" sc2/src/uqm/rust_comm.c`
```
564:    rust_ClearSubtitles ();
570:    rust_CheckSubtitles ();
576:    rust_RedrawSubtitles ();
724:        rust_RedrawSubtitles ();
```
**CONFIRMED**: Circular routing at lines 564/570/576 (plus line 724). All three subtitle bridge functions route back to Rust FFI instead of C drawing primitives.

### CS (Summary) — `grep -n "not yet wired|not yet implemented|for now" rust/src/comm/ffi.rs rust/src/comm/talk_segue.rs`
```
ffi.rs:879:    // Advance through pages until Exit or Abort (abort not yet wired — use
ffi.rs:881:    // input handling is not yet implemented).
talk_segue.rs:1002:        // The colormap ptr comes from the C CommData; pass null for now.
```
**CONFIRMED**: Stale comments at ffi.rs:879-881 inside `rust_ShowConversationSummary` production body. `rust_ShowConversationSummary` runs the Rust SummaryView loop (no C rendering) in all builds.

### RL (Lock) — read `ffi.rs:715-752`
```
pub unsafe extern "C" fn rust_DoCommunication() -> c_int {
    ...
    let mut state = COMM_STATE.write();
    match do_communication(&mut state) {
        CommunicationResult::Continue => {
            if state.is_talking_finished() && state.responses().count() > 0 {
                if let Some(selected_result) = {
                    let check_result = player_response_input(&mut state);  // SECOND call
                    if check_result == PlayerInputResult::Selected {
                        select_response(&mut state)
                    } else { None }
                } {
                    let (func, rref) = selected_result;
                    drop(state);
                    func(rref);
                    return 1;
                }
            }
            1
        }
    }
}
```
**CONFIRMED**: `do_communication` (line 723) already calls `player_response_input` internally, then the FFI wrapper calls it again (line 735) — double-call pattern confirmed. Lock discipline: `drop(state)` before `func(rref)` exists but only on the second-call path; the first `do_communication` call path may have already consumed the Selected result.

### SM (Stale Markers) — full grep sweep of production comm/*.rs
Production stale markers found:
```
ffi.rs:879  — "abort not yet wired"  (production body of rust_ShowConversationSummary)
ffi.rs:881  — "input handling is not yet implemented"  (same function)
talk_segue.rs:1002  — "pass null for now"  (production set_colormap)
```
False positives (excluded from REQ-SM-001 scope per REQ-SM-002):
```
glue.rs:67      — doc comment on #[cfg(test)] function: "Test-mode resolve_phrase: returns placeholder strings"
phrase_state.rs:30 — doc comment describing behavior: "not yet disabled this encounter"
state.rs:88     — doc comment describing C equivalence: "~0 = not yet initialized"
```
**Net production stale markers: 3** (all in ffi.rs:879/881 and talk_segue.rs:1002 — all covered by existing requirements).

### DC (State machine) — read `ffi.rs:721-752`
**CONFIRMED**: Double `player_response_input` call pattern verified (see RL section above). `do_communication()` at line 723 calls `player_response_input` internally via `talk_segue`. The outer FFI wrapper at line 735 calls it a second time to detect and dispatch Selected. This is the defect described in REQ-DC-003 / specification §1 item 3.

### E2E — `cd rust && cargo test --lib -- comm`
```
test result: ok. 268 passed; 0 failed; 0 ignored; 0 measured; 2215 filtered out; finished in 0.02s
```
**CONFIRMED**: All 268 comm tests pass.

---

## Requirement Mapping Summary

| Family | Req IDs | Code Location Verified | Testability Criterion |
|---|---|---|---|
| CM | REQ-CM-001/002/003 | `talk_segue.rs:1003` null_mut confirmed | Build + runtime manual (colormap visible) |
| MU | REQ-MU-001/002/003 | `talk_segue.rs:945` null_mut confirmed | Build + runtime manual (music plays) |
| SD | REQ-SD-001/002/003/004/005 | `rust_comm.c:564/570/576/724` circular routing confirmed | Build + C function trace + runtime manual (subtitles appear) |
| CS | REQ-CS-001/002/003 | `ffi.rs:879-881` stale comments + Rust SummaryView active in production confirmed | Build + unit test cfg guard |
| RL | REQ-RL-001/002/003/004 | `ffi.rs:715-752` double player_response_input + fragile lock-drop confirmed | Rust unit test: select→extract→drop→callback |
| DC | REQ-DC-001/002/003/004/005 | `ffi.rs:723/735` double-call state machine confirmed | Rust unit test: state machine transitions |
| TS | REQ-TS-001/002/003/004 | Intro sequence in `talk_segue.rs:309-322` — defects upstream (CM/MU) affect TS | Build + runtime manual |
| SM | REQ-SM-001/002 | 3 production markers confirmed, 3 false positives excluded | Automated grep sweep: zero hits post-fix |
| E2E | REQ-E2E-001 through 007 | 268 tests pass; runtime gaps covered by CM/MU/SD/RL/DC fixes | cargo test --lib -- comm; runtime |

### No Missing Requirements
Cross-check against specification.md §1 five gaps:
1. Null colormap handle → REQ-CM-001/002/003 [OK]
2. Null music handle → REQ-MU-001/002/003 [OK]
3. Subtitle rendering disconnection → REQ-SD-001/002/003/004/005 [OK]
4. DoCommunication response dispatch / lock discipline → REQ-RL-001/002/003/004 + REQ-DC-001/002/003/004/005 [OK]
5. Stale markers + dead summary code → REQ-SM-001/002 + REQ-CS-001/002/003 [OK]

Cross-check against domain-model.md §4 integration touchpoints:
- `talk_segue.rs` — covered by REQ-CM, REQ-MU, REQ-RL, REQ-TS, REQ-SM [OK]
- `ffi.rs` — covered by REQ-RL, REQ-DC, REQ-CS, REQ-SM [OK]
- `rust_comm.c` — covered by REQ-SD, REQ-CM, REQ-MU [OK]
- `rust_comm.h` — new declarations implied by REQ-CM/MU/SD (no additional requirements needed) [OK]
- `comm.c` — covered by REQ-SD-002/003/004 [OK]

All 5 specification gaps covered. All domain-model integration touchpoints addressed. **No missing requirements.**

---

## GATE DECISION: PASS

All success criteria met:
- [x] Every requirement in `requirements.md` maps to a specific code location verified by grep/read
- [x] Every requirement has a testable verification criterion (build, unit test, grep sweep, or runtime manual)
- [x] No missing gaps — all 5 specification defects and all domain-model touchpoints are covered
- [x] Requirements document is frozen — no changes to `requirements.md` after this phase

**Requirements are locked. P00a and subsequent phases may proceed.**
