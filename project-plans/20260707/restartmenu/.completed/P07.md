# P07: RestartMenu Orchestration — PASS (Revised)

## Files Created
- `rust/src/mainloop/restart_menu/orchestration.rs` (~630 lines)
  + restart_menu_impl() — matches C RestartMenu() exactly
  + try_start_game_impl() — matches C TryStartGame() exactly
  + start_game_impl() — matches C StartGame() exactly
  + rust_start_game() — C entry point (#[no_mangle] extern "C")
  + 13 Tier-1 tests with MockOps

## Files Modified
- `rust/src/mainloop/restart_menu/mod.rs` — Added orchestration module
- `rust/src/mainloop/restart_menu/c_extern.rs` — Added Color::white_gray(), menu state lifecycle externs
- `rust/src/mainloop/restart_menu/restart_ops.rs` — Added create_menu_state, set_menu_priv_data, destroy_menu_state to trait and CffiOps
- `rust/src/mainloop/restart_menu/do_restart.rs` — Added MockOps stubs for new trait methods
- `sc2/src/uqm/rust_bridge_restart.c` — Added uqm_create_menu_state, uqm_set_menu_priv_data, uqm_destroy_menu_state
- `sc2/src/uqm/rust_bridge_restart.h` — Prototypes for new functions

## Deepthinker P07a Fixes (5 issues)
1. [CRITICAL] MENU_STATE lifecycle: Added create/set_priv_data/destroy to trait + CffiOps + C bridge. try_start_game_impl now creates MENU_STATE, sets InputFunc and privData, destroys on all exit paths.
2. [MAJOR] Drawable cleanup: Changed to release_drawable → destroy_drawable (matches C ReleaseDrawable→DestroyDrawable).
3. [MAJOR] Menu state init: Moved full init to try_start_game_impl. restart_menu_impl only resets initialized flag (preserves cur_state for Super Melee loop).
4. [MINOR] CHECK_LOAD comment: Fixed to 1<<4.
5. [MINOR] TODO test removed.

## Verification
- cargo build --lib: zero errors
- cargo test --lib mainloop::restart_menu -- --test-threads=1: 77 passed
- cargo test --lib -- --test-threads=1: 2722 passed
- C wrapper syntax check: clean (-Wall, zero errors)
- Zero TODO/FIXME/HACK markers

VERDICT: PASS
