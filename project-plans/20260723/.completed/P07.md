# Phase 07a: Verify Presentation and Capture — PASS

Phase ID: `PLAN-20260723-RUNTIME-AUTOMATION.P07.VERIFY`

**Verdict: Phase 07: PASS**

Independent verification executed on 2026-07-23. This is a clean, full verification with no assumptions from prior assessments.

## 1. Source Inspection Summary

**Files verified:**
- `rust/src/automation/capture.rs` — pure present-call observation and locked logical capture model
- `rust/src/automation/mod.rs` — module root with capture re-exports
- `rust/src/automation/scheduler.rs` — CaptureGeneration and validate_capture_completion (P02 dependency)
- `rust/src/automation/trace.rs` — TraceRecord and RecordKind types (P03 dependency)
- `rust/src/automation/artifact.rs` — label validation via confine_artifact_path (P03 dependency)

**No C graphics files edited by P07.** The C changes in `gameinp.c` and `rust_comm.c` are from P06 (automation input hooks) and P00 (debug/lint remediation). P07 created only the new `rust/src/automation/capture.rs` file (untracked) and modified `rust/src/automation/mod.rs` (untracked directory).

**rust_gfx_postprocess user edit preserved.** The `ffi.rs` diff shows only P00 lint-remediation safety doc comments; the texture upload body is intact and integrated.

## 2. Requirement Verification

### REQ-PRESENT-001: Present count and observe exactly once after Canvas::present() returns normally
- `classify_present(skip_swap, force_redraw, bbox_valid)` returns `Normal` or `ForcedRedraw` for counting.
- `should_count_present` returns `true` for `Normal` and `ForcedRedraw`.
- Test coverage: `classify_normal_present`, `classify_forced_redraw`.
- **VERIFIED.**

### REQ-PRESENT-002: No-redraw early return and SkipSwap do not count or complete capture
- `classify_present` returns `SkipSwap` when `skip_swap=true`, `NoRedraw` when `bbox_valid=false`.
- `should_count_present` returns `false` for both `SkipSwap` and `NoRedraw`.
- SkipSwap takes priority over ForcedRedraw.
- Test coverage: `classify_skip_swap`, `classify_no_redraw`, `skip_swap_takes_priority_over_redraw`.
- **VERIFIED.**

### REQ-SHOT-001: CaptureMetadata::standard_320x240
- `surface_index=0`, `width=320`, `height=240`.
- `window_scaling_may_be_absent=true`, `overlays_may_be_absent=true`, `direct_video_may_be_absent=true`.
- Test coverage: `standard_metadata_320x240`.
- **VERIFIED.**

### REQ-SHOT-002: validate_surface checks
- Checks positive width/height (`InvalidDimensions`), positive pitch (`InvalidPitch`), BPP=32 and bytes_per_pixel=4 (`UnsupportedBpp`), checked i64 size computation pitch*height (`InvalidSize`).
- Does NOT use hand-written partial `SDL_Surface`. Uses typed `SurfaceMetadata` struct.
- No `NullPixels` check in validate_surface (caller must verify pointer), but `NullPixels` error variant exists for the external layer.
- Test coverage: `valid_surface_passes`, `zero_width_fails`, `negative_height_fails`, `zero_pitch_fails`, `unsupported_bpp_fails`, `max_i32_size_succeeds`.
- **VERIFIED.**

### REQ-SHOT-003: Real SDL_MUSTLOCK/LockSurface/UnlockSurface
- This is P07 pure model; linked harness is P08.
- `SurfaceMetadata` contains all fields needed for lock-required surfaces (bpp, bytes_per_pixel, pitch, masks).
- No assumption of no-lock. The model validates metadata; locking is external (P08).
- **VERIFIED (pure model scope).**

### REQ-SHOT-004: attempt_capture_completion validates generation
- Calls `validate_capture_completion` from P02 (scheduler.rs).
- Validates: nonzero, not stale, not duplicate, not future, not zero.
- Only exact `Match` can complete; all others return `GenerationMismatch`.
- NotArmed when `!pending.is_armed()`.
- Test coverage: `capture_match_completes`, `capture_stale_fails`, `capture_duplicate_fails`, `capture_future_fails`, `capture_not_armed`, `capture_zero_generation_fails`.
- **VERIFIED.**

### REQ-SHOT-005: Safe-row-copy uses min(row_bytes, pitch)
- `safe_row_copy(width, pitch, bytes_per_pixel)` returns `Some(row.min(p))` where `row = row_bytes(width, bytes_per_pixel)`.
- Exclusive collision-safe naming: label is passed as parameter to `capture_trace_record`.
- Label sanitization is in P03's `artifact.rs::confine_artifact_path` via `is_valid_label`.
- Test coverage: `safe_row_copy_clamps_to_pitch`, `padded_pitch_more_than_row_bytes`.
- **VERIFIED.**

### REQ-SHOT-006: CaptureCompletion::Failure is terminal
- `CaptureCompletion::Failure` is a distinct variant from `CaptureCompletion::Completed { generation }`.
- Can never equal `Completed`. Any I/O/record failure returns `Failure`, not `Completed`.
- Test coverage: `capture_failure_does_not_complete`.
- **VERIFIED.**

### REQ-TRACE-001: Present/capture trace records
- `present_trace_record` uses `RecordKind::Presentation` with `sequence`, `elapsed_ms`, `present_seen` fields.
- `capture_trace_record` uses `RecordKind::Capture` with `sequence`, `elapsed_ms`, label=`{label}_gen{N}`.
- Test coverage: `present_trace_record_correct_kind`, `capture_trace_record_correct_kind`, `skip_swap_trace_label`.
- **VERIFIED.**

### REQ-FFI-001/004: Pure model does not hold runtime/IO locks during graphics/SDL
- `capture.rs` has no `Mutex`, `RwLock`, `parking_lot`, `std::sync`, or lock types.
- Pure model: all functions take typed inputs and return typed outputs with no side effects.
- No lock ordering violation possible in the pure types.
- **VERIFIED.**

### Item 11: No raw copy ignores pitch/masks/checked lengths
- `safe_row_copy` uses `min(row, pitch)` — checked.
- `row_bytes` uses `u32::try_from(width)` then `checked_mul`.
- `pixel_data_size` uses `row_bytes` then `checked_mul` for height.
- All use checked arithmetic throughout.
- **VERIFIED.**

### Item 12: No partial SDL_Surface layout
- `SurfaceMetadata` is a typed struct with explicit fields (width, height, pitch, bpp, bytes_per_pixel, masks).
- Not derived from the hand-written `SDL_Surface` in `graphics/ffi.rs` (which has `format: *mut c_void`).
- The capture module explicitly documents: "All fields are derived from ABI-authoritative SDL accessors, not the hand-written partial SDL_Surface format pointer."
- **VERIFIED.**

## 3. Strict Gates (all exit 0)

| Gate | Command | Exit | Notes |
|------|---------|------|-------|
| check | `cargo check --workspace --all-features` | 0 | |
| fmt | `cargo fmt --all --check` | 0 | |
| clippy | `cargo clippy --workspace --all-targets --all-features -- -D warnings` | 0 | |
| test | `cargo test --lib --workspace --all-features -- --test-threads=1` | 0 | 3040 passed, 0 failed, 6 ignored |

**Focused tests:** `cargo test --lib automation::capture` → 26 passed, 0 failed.

**Pre-existing flaky test:** `threading::tests::test_thread_system_init` fails under parallel test execution due to global thread state mutation (documented since P01). Passes in isolation (`--exact`) and with `--test-threads=1`. Not caused by P07.

## 4. Mutation Test Results

All mutations applied to `/tmp/p07a-mutations/` copies. Original tree preserved (verified with `diff`).

| # | Mutation | Result | Caught By |
|---|----------|--------|-----------|
| M1 | should_count returns true for SkipSwap | CAUGHT | `classify_skip_swap`, `skip_swap_takes_priority_over_redraw` |
| M2 | should_count returns true for NoRedraw | CAUGHT | `classify_no_redraw` |
| M3 | stale generation completes | CAUGHT | `capture_stale_fails` |
| M4 | zero generation completes | CAUGHT | `capture_zero_generation_fails` |
| M5 | duplicate generation completes | CAUGHT | `capture_duplicate_fails` |
| M6 | invalid BPP accepted | CAUGHT | `unsupported_bpp_fails` |
| M7 | negative dimensions accepted | CAUGHT | `negative_height_fails` |
| M8 | overflow size accepted | ESCAPED | Unreachable: i32×i32 cannot overflow i64; `checked_mul` is correct defensive code |
| M9 | wrong trace kind (Presentation→Capture) | CAUGHT | `present_trace_record_correct_kind` |

**Summary: 8/9 mutations caught.** M8 escaped because `i32::MAX * i32::MAX = 4.61×10^18 < i64::MAX (9.22×10^18)`, making the `InvalidSize` overflow path mathematically unreachable. The production code correctly uses `checked_mul` defensively; no test can exercise this path. This is a benign coverage gap, not a code defect.

## 5. Deferred P08 Integrations

The following are explicitly deferred to P08 and not claimed by P07:
- Real linked `SDL_MUSTLOCK` / `SDL_LockSurface` / `SDL_UnlockSurface` integration
- Production-linked present harness with `TFB_InitGraphics` (single call, DCQ init)
- Real lock-required surface creation and byte verification
- Forced-lock-failure/no-read fault seam
- Full linked graphics harness with `nm -A` evidence
- REQ-TEST-003 (full production-linked graphics harness)
- REQ-SEM-002 (real machine movement)

## 6. Preservation Evidence

- Original `capture.rs` restored after all mutation tests (verified with `diff`).
- No source fixes or waivers applied.
- `rust_gfx_postprocess` user edit preserved in `graphics/ffi.rs`.
- No C graphics files edited by P07.
- No `.completed/P07.md` existed before this verification (P07 worker does not create markers).
