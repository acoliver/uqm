# P13: Summary Guard TDD — Stale Marker Tests

**Phase ID:** P13  
**Date:** 2026-03-28  
**Plan:** PLAN-20260325-COMMPT3.P13  
**Requirements:** REQ-CS-002, REQ-CS-003, REQ-SM-001, REQ-SM-002  
**Pseudocode:** 004-summary-guard-stale-markers lines 01-47

## Summary

Added five P13 tests to `rust/src/comm/talk_segue.rs` `mod tests`:

1. `verify_production_delegates_to_c_p13` — verifies `#[cfg(not(test))]`
   version of `rust_ShowConversationSummary` calls `c_SelectConversationSummary`.
2. `verify_no_summaryview_in_production_p13` — verifies `SummaryView` does NOT
   appear in the production function body.
3. `verify_zero_stale_markers_ffi_p13` — sweeps `ffi.rs` for stale markers
   outside test/doc/exempt lines. Uses `compute_test_lines()` helper to correctly
   exclude lines inside `mod tests { ... }` blocks. Case-insensitive matching.
4. `verify_zero_stale_markers_talk_segue_p13` — same sweep for `talk_segue.rs`.
5. `verify_exemptions_valid_p13` — confirms exemption strings exist in their
   respective source files.

Added `compute_test_lines()` helper to track which source lines are inside
`mod tests { ... }` blocks using brace-depth counting.

## Files Changed

- `rust/src/comm/talk_segue.rs` — added P13 test block (~160 lines) and
  `compute_test_lines()` helper before existing source-inspection helpers.

## Verification

All 5 P13 tests pass:

```
test comm::talk_segue::tests::verify_exemptions_valid_p13 ... ok
test comm::talk_segue::tests::verify_no_summaryview_in_production_p13 ... ok
test comm::talk_segue::tests::verify_production_delegates_to_c_p13 ... ok
test comm::talk_segue::tests::verify_zero_stale_markers_ffi_p13 ... ok
test comm::talk_segue::tests::verify_zero_stale_markers_talk_segue_p13 ... ok
```
